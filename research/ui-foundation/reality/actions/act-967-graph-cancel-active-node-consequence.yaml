id: ACT-967
implementation_status: present
capability_status: current
executable: true
command_id: CMD-905
actor: "REST caller; this is the affected graph-node consequence of CMD-905, not an independent node command."
permission_requirements: [PER-900, PER-901, PER-902, PER-903]
preconditions: ["ACT-970 has transitioned the graph run to cancelling and ACT-966 is applying its terminal graph-run consequence for ACT-932's accepted REST cancel.", "The affected graph node is leased, running, or suspended with an active/suspended lease."]
expected_source_version: "No client graph version is accepted; node cancellation is emitted during the same graph cancellation application."
required_input: {run_id: required, affected_node_id: required}
validation: ["CMD-905 cancellation accepts the routed legacy lifecycle state", "PER-903 graph lease/node cancellation validation"]
durable_effect: "Graph cancellation revokes the affected lease and transitions the affected graph-node carrier to cancelled without changing a legacy task or row carrier."
resulting_state_id: STA-931
failure_modes: ["404 missing run/graph/node", "409 illegal or already-applied cancellation state", "executor cancellation failure"]
stale_state_behavior: "A later or duplicate cancellation observes the current graph node/lease state; it does not invent a cross-carrier recovery transition."
idempotency: "No key; node and lease state make repeated cancellation application domain-dependent."
retry_behavior: "Refresh graph node, lease, and graph-run projections; do not replay this consequence independently of cancellation."
reversibility: "No direct node uncancel exists; any recovery path is separate graph work."
audit_evidence_ids: [EVI-913, EVI-931, EVI-932, EVI-933]
transition: {from_state_ids: [STA-970, STA-971, STA-972], to_state_id: STA-931}
carrier_relationship: {kind: paired graph-node cancellation consequence, paired_graph_run_entry_action_id: ACT-970, paired_graph_run_terminal_action_id: ACT-966, rest_acceptance_action_id: ACT-932, command_id: CMD-905, excluded_source_state_ids: [STA-926], ordering: "Only leased, running, or suspended nodes are directly cancelled by CMD-905 evidence. The graph node transitions on its own carrier while ACT-970/ACT-966 transition the graph run; neither action transitions to the legacy row."}
enforced_authentication: "PER-900 optional global valid-bearer gate."
enforced_authorization: absent
domain_eligibility: "PER-902 REST cancellation eligibility and PER-903 graph node/lease cancellation rules."
proposed_role_policy: null
