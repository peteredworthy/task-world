id: ACT-3
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-25
actor: caller
permission_requirements:
- PER-1
- PER-7
preconditions:
- repository registered
expected_source_version: None
required_input:
  name: required
validation:
- entry exists
durable_effect: Synchronously unlinks symlink or recursively removes repository directory.
resulting_state_id: STA-51
failure_modes:
- '404'
- filesystem error
stale_state_behavior: No active-run/version guard.
idempotency: Second remove not found.
retry_behavior: Inspect entry/dependents.
reversibility: Destructive; no undo.
audit_evidence_ids:
- EVD-35
transition:
  from_state_id: STA-50
  to_state_id: STA-51
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-7 repository prerequisite
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: unexercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-3 implementation status present: ACT-3 Synchronously unlinks symlink or recursively
    removes repository directory. is present: SER rows SER-0039 inspect src/orchestrator/api/routers/repos.py::remove_repository
    and establish the record proposition under EVD-35.'
  test: 'ACT-3 test status unexercised: ACT-3 Synchronously unlinks symlink or recursively removes repository
    directory. remains unexercised because no admitted exact test locator asserts its complete record
    proposition under EVD-35.'
  documentation: 'ACT-3 documentation status documented: ACT-3 Synchronously unlinks symlink or recursively
    removes repository directory. is documented at reality/actions/act-3-repository-remove.yaml and by
    EVD-35; any listed question (Q-5 (open: Define product-role authorization before treating actor text
    as authority)) is explicitly disclosed and does not create contradictory documentation for this bounded
    proposition.'
  capability: 'ACT-3 capability status unknown: ACT-3 Synchronously unlinks symlink or recursively removes
    repository directory. remains unknown as a user capability: no CAP implementation_carrier_binding;
    EVD-35 establishes the record carrier but no relevant CAP authority classifies this record as current
    or derived.'
  epistemic: 'ACT-3 epistemic status observed: ACT-3 Synchronously unlinks symlink or recursively removes
    repository directory. is observed from direct/audit authority EVD-35 (EVD-35 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#repository-review-sourceconfiguration-and-file-effects)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/repos.py::remove_repository
test_locators: []
bounded_test_locators: []
