id: ACT-6
title: Create run
definition: Synchronously create a legacy run from absent identity, return the created-response carrier,
  and persist a distinct draft row.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-1
actor: caller
permission_requirements:
- PER-1
- PER-2
- PER-3
preconditions:
- Exactly one routine source
- selectable runner and valid configuration
expected_source_version: No source row exists for the new run identity.
required_input:
  routine_source: required
  runner: required
validation:
- CreateRunRequest schema
- runner selection validation
durable_effect: Creates a run row and creation events; the synchronous response confirms creation, and
  the durable row is draft.
resulting_state_id: STA-57
failure_modes:
- 422 malformed request
- 404 routine
- runner unavailable
stale_state_behavior: No source version; duplicate creation creates distinct runs.
idempotency: No idempotency key.
retry_behavior: Retry after unknown response can create another run.
reversibility: Delete is separate and status-limited.
audit_evidence_ids:
- EVD-43
- EVD-38
- EVD-39
- EVD-40
transition:
  from_state_id: STA-56
  to_state_id: STA-57
created_draft_transition:
  from_state_id: STA-57
  to_state_id: STA-1
  evidence_ids:
  - EVD-43
enforced_authentication: Optional global valid-bearer gate; disabled by default.
enforced_authorization: absent
domain_eligibility: No pre-existing source row for this creation; exactly one routine source, selectable
  runner, valid configuration, and request schema.
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: ACT-6 creates a draft run and creation evidence.
  test: The API test asserts successful creation and returned draft identity.
  documentation: ACT-6 create contract is documented.
  capability: Creation does not prove subsequent runner availability.
  epistemic: ACT-6 is observed by EVD-43/EVD-38-40.
implementation_locators:
- src/orchestrator/api/routers/runs.py::create_run
test_locators:
- tests/integration/test_api_runs.py::test_create_run
