{id: ACT-982, implementation_status: present, capability_status: current, executable: true, command_id: CMD-918, actor: caller, permission_requirements: [PER-900, PER-902], preconditions: ["run is active or paused", "worktree and source branch exist"], expected_source_version: "None; no client branch version", required_input: {run_id: required}, validation: ["run status", "worktree", "source branch"], durable_effect: "The review-route back-merge invokes the same synchronous source-to-run branch merge; clean merge creates a git merge effect and conflicts remain unresolved.", resulting_state_id: STA-938, failure_modes: ["409 ineligible run", "400 missing branch/worktree", "git conflict", "git error"], stale_state_behavior: "No branch version fences a concurrent source/run branch change.", idempotency: "No key; git ancestry determines repeated effect.", retry_behavior: "Inspect branch and conflict state before retry.", reversibility: "Only the qualifying HEAD merge has the separate revert-back-merge compensation.", audit_evidence_ids: [EVI-922], transition: {from_state_id: STA-938, to_state_id: STA-938}, enforced_authentication: "optional JWT", enforced_authorization: absent, domain_eligibility: "PER-902 run-state and branch prerequisites", proposed_role_policy: null}
