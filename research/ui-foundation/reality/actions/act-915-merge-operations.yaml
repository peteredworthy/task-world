{id: ACT-915, implementation_status: present, capability_status: current, executable: true, command_id: CMD-903, actor: caller, permission_requirements: [PER-900], preconditions: ["operation-specific branch/readiness state"], expected_source_version: "None", required_input: {run_id: required, operation: "back_merge|revert_back_merge|merge_back"}, validation: ["active/paused back-merge; completed/no failed gates merge-back; HEAD merge for revert"], durable_effect: "Mutates git branches/commits and may leave conflict state.", resulting_state_id: STA-927, failure_modes: ["git conflict", "409 readiness/state", "git error"], stale_state_behavior: "No branch/source version; concurrent git changes can conflict.", idempotency: "Git-dependent; no key", retry_behavior: "Inspect branch/conflict state before retry.", reversibility: "Only revert-back-merge reverses one qualifying worktree-head merge.", audit_evidence_ids: [EVI-903], transition: {from_state_id: STA-927, to_state_id: STA-927}, enforced_authentication: "optional JWT", enforced_authorization: absent, domain_eligibility: "operation-specific git/workflow state", proposed_role_policy: null}
