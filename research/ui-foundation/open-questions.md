# Open questions

- **Q-1 — Choose and migrate canonical attempt identity** (blocking): Implement or verify the named contract with fresh source and test evidence.
- **Q-2 — Define typed cross-mode conversion and selection contracts** (blocking): Implement or verify the named contract with fresh source and test evidence.
- **Q-3 — Reconcile JSONL authority documentation with executable SQL authority** (blocking): Implement or verify the named contract with fresh source and test evidence.
- **Q-4 — Define a live-WAL-safe backup and replay contract** (blocking): Implement or verify the named contract with fresh source and test evidence.
- **Q-5 — Define product-role authorization before treating actor text as authority** (blocking): Implement or verify the named contract with fresh source and test evidence.
- **Q-6 — Decide whether execution-bound graph MCP patch macros need an action contract** (blocking): Implement or verify the named contract with fresh source and test evidence.
- **Q-7 — Hash-guard decisive implementation and test evidence** (blocking): Implement or verify the named contract with fresh source and test evidence.
