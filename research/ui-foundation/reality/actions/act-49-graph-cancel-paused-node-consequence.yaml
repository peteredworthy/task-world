id: ACT-49
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-6
actor: REST caller; this is the affected graph-node consequence of CMD-6, not an independent
  node command.
permission_requirements:
- PER-1
- PER-2
- PER-3
- PER-4
preconditions:
- ACT-51 has transitioned the graph run to cancelling and ACT-48 is applying its terminal
  graph-run consequence for ACT-21's accepted REST cancel.
- The affected graph node is leased, running, or suspended with an active/suspended
  lease.
expected_source_version: No client graph version is accepted; node cancellation is
  emitted during the same graph cancellation application.
required_input:
  run_id: required
  affected_node_id: required
validation:
- CMD-6 cancellation accepts the routed legacy lifecycle state
- PER-4 graph lease/node cancellation validation
durable_effect: Graph cancellation revokes the affected lease and transitions the
  affected graph-node carrier to cancelled without changing a legacy task or row carrier.
resulting_state_id: STA-35
failure_modes:
- 404 missing run/graph/node
- 409 illegal or already-applied cancellation state
- executor cancellation failure
stale_state_behavior: A later or duplicate cancellation observes the current graph
  node/lease state; it does not invent a cross-carrier recovery transition.
idempotency: No key; node and lease state make repeated cancellation application domain-dependent.
retry_behavior: Refresh graph node, lease, and graph-run projections; do not replay
  this consequence independently of cancellation.
reversibility: No direct node uncancel exists; any recovery path is separate graph
  work.
audit_evidence_ids:
- EVD-20
- EVD-38
- EVD-39
- EVD-40
transition:
  from_state_ids:
  - STA-30
  - STA-31
  - STA-32
  to_state_id: STA-35
carrier_relationship:
  kind: paired graph-node cancellation consequence
  paired_graph_run_entry_action_id: ACT-51
  paired_graph_run_terminal_action_id: ACT-48
  rest_acceptance_action_id: ACT-21
  command_id: CMD-6
  excluded_source_state_ids:
  - STA-71
  - STA-72
  ordering: Only leased, running, or suspended nodes are directly cancelled by CMD-6
    evidence. The graph node transitions on its own carrier while ACT-51/ACT-48 transition
    the graph run; neither action transitions to the legacy row.
enforced_authentication: PER-1 optional global valid-bearer gate.
enforced_authorization: absent
domain_eligibility: PER-3 REST cancellation eligibility and PER-4 graph node/lease
  cancellation rules.
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: unexercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-49 implementation status present: ACT-49 Graph cancellation revokes the affected
    lease and transitions the affected graph-node carrier to cancelled without changing a legacy task
    or row carrier. is present: SER rows SER-0079 inspect src/orchestrator/graph/commands/lifecycle.py::handle_cancel
    and establish the record proposition under EVD-20, EVD-38, EVD-39, EVD-40.'
  test: 'ACT-49 test status unexercised: ACT-49 Graph cancellation revokes the affected lease and transitions
    the affected graph-node carrier to cancelled without changing a legacy task or row carrier. remains
    unexercised because no admitted exact test locator asserts its complete record proposition under EVD-20,
    EVD-38, EVD-39, EVD-40.'
  documentation: 'ACT-49 documentation status documented: ACT-49 Graph cancellation revokes the affected
    lease and transitions the affected graph-node carrier to cancelled without changing a legacy task
    or row carrier. is documented at reality/actions/act-49-graph-cancel-paused-node-consequence.yaml
    and by EVD-20, EVD-38, EVD-39, EVD-40; any listed question (no canonical unresolved conflict or question)
    is explicitly disclosed and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-49 capability status unknown: ACT-49 Graph cancellation revokes the affected lease
    and transitions the affected graph-node carrier to cancelled without changing a legacy task or row
    carrier. remains unknown as a user capability: no CAP implementation_carrier_binding; EVD-20, EVD-38,
    EVD-39, EVD-40 establishes the record carrier but no relevant CAP authority classifies this record
    as current or derived.'
  epistemic: 'ACT-49 epistemic status observed: ACT-49 Graph cancellation revokes the affected lease and
    transitions the affected graph-node carrier to cancelled without changing a legacy task or row carrier.
    is observed from direct/audit authority EVD-20, EVD-38, EVD-39, EVD-40 (EVD-20 (audit-report: research/ui-foundation/agent-reports/02-graph-runtime.md#command_specs-exhaustive-command-to-effect-table);
    EVD-38 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#authority-model);
    EVD-39 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#authority-model);
    EVD-40 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#legacy-versus-graph-mode)),
    with no canonical conflict/question qualification; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/graph/commands/lifecycle.py::handle_cancel
test_locators: []
bounded_test_locators: []
