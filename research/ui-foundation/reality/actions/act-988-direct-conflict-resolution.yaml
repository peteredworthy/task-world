{id: ACT-988, implementation_status: present, capability_status: current, executable: true, command_id: CMD-922, actor: caller, permission_requirements: [PER-900, PER-902], preconditions: ["run worktree exists", "file is currently unresolved"], expected_source_version: "None; no conflict-file version", required_input: {run_id: required, file_path: required, resolutions: required}, validation: ["file belongs to git unresolved list", "choice is ours/theirs/manual", "manual choice has content"], durable_effect: "Synchronously writes selected resolutions, removes markers, stages the file, and emits ConflictResolved with remaining count.", resulting_state_id: STA-940, failure_modes: ["404 non-conflict file", "422 invalid choice/manual content", "git/filesystem error"], stale_state_behavior: "The conflict list can change between read and submit; no expected version is accepted.", idempotency: "No key; repeat depends on remaining conflict markers.", retry_behavior: "Refresh unresolved files and submit only currently unresolved blocks.", reversibility: "Staging a selected resolution is not a completed merge and has no endpoint undo.", audit_evidence_ids: [EVI-923], transition: {from_state_id: STA-939, to_state_id: STA-940}, enforced_authentication: "optional JWT", enforced_authorization: absent, domain_eligibility: "PER-902 unresolved-file and resolution-shape prerequisites", proposed_role_policy: null}
