# Graph Run Remediation Requirements Ledger

This ledger resumes the approved plan in
`docs/plans/2026-08-03-graph-run-remediation.md`. It records functional proof,
not patch activity. `orchestrator.db` and `worktrees/r359` are excluded from all
work.

| ID | Required behavior | Acceptance and product-real proof | Current evidence | Status | Remaining gap |
|---|---|---|---|---|---|
| R1 | `/graph/health` and its compact source are byte-bounded for candidate identities. | Persist an over-budget `candidate_id`, fetch health through the HTTP API, and prove the stored compact row and response contain a deterministic bounded/hash identity while counts and deduplication remain correct. | Validated 2026-08-04: real ASGI/in-memory SQLite proof covered ASCII/UTF-8 live and rebuild redaction, exact 200/201-byte boundary, hashed-vs-literal namespace separation, repeated-long latest verdicts, legacy-heavy parity, and complete-looking pre-R1 row rejection without replay. Regression gate: 77 passed; Ruff clean; Pyright 0 errors/warnings. | validated | None. |
| R2 | Every graph read and returned payload is explicitly bounded or paginated. | Exercise each public graph/read-model endpoint against oversized histories and payloads; verify bounds, continuation/truncation metadata, and no full-history materialization on bounded paths. | Validated 2026-08-05 under the documented simplification tradeoff in `2026-08-05-graph-run-remediation-simplification.md`: archival append performs only a dirty-position update; background maintenance folds authority through fixed keyset batches into one permitted current projection, writes topology/blocker/region rows in fixed batches under a rollback-safe savepoint, derives regions through SQL keyset/count/capped-prefix queries, and publishes one shared position last. Archival-only generators cap `record_ids`, `record_bound_positions`, `bound_records`, and `support_ids` before payload construction with exact totals, stable cursors, truthful truncation, and null whole-list SHA/byte fields. Node detail retains one normalized bounded fact owner and uses scalar counts plus capped SQL prefix reads; custom SHA/chunk/subcursor state was removed. Artifact GC uses a project-scoped scalar reference join and does not enumerate surviving runs or hashes. Product-real ASGI proofs cover 251-item nested values, corrupt falsified size metadata rejected before decode, rollback preserving the previous generation while every route returns retryable 503, all-page live/delete/rebuild parity, failed-outbox budgets, expected-position 409, and stale-owner 503. Independent evidence includes targeted archival/node/GC, graph API/read-contract/node, runtime, artifact, and controller suites plus source audit across graph APIs, artifacts, runtime, dispatch, controller, service, recovery, and maintenance. Frontend 409/503 behavior passed 28 focused tests. Final schema proof uses a fresh file database created directly from ORM metadata, including the bounded blocker-region index; no schema-upgrade path or migration bookkeeping remains. | validated | None. |
| R3 | Pause/resume preserves exactly one graph-driver owner and recovers interrupted work. | Pause during a real active lease, observe stable quiescence, resume through the signal API, and prove one driver/no duplicate dispatch plus deterministic lease recovery. | Validated 2026-08-04: real temporary SQLite signal-queue proof started one driver, paused by cancel-and-await, resumed exactly one replacement driver, and ignored a stale active RESUME (`started=[1,2]`, `stopped=[1,2]`, generation 2). Generation-fenced cleanup prevents an old task clearing newer ownership. Regression/static gate: 25 passed; Ruff, format, and Pyright clean. | validated | None. |
| R4 | A graph run reports completed only after truthful durable finalization. | Complete a run in a real temporary repository; changed work has a non-empty end commit and clean tree, no-change is explicit, and commit failure prevents completed state with a recoverable typed error. | Validated 2026-08-04: real temporary Git/file-SQLite proofs covered changed finalization before completion, explicit no-change receipt (`created_commit=False`), real index-lock failure retaining non-completed paused state with typed error, and the crash-window retry. The retry reuses one durable finalization receipt and emits exactly one completion transition. Regression/static gate: 20 graph-driver tests; Ruff, format, and Pyright clean. | validated | None. |
| R5 | Merge disposition is explicit and truthful in API and UI. | Exercise merged, ready, no-change, dirty/unfinalized, and blocked runs through API and rendered UI; merge rejects non-ready states with a clear reason. | Validated 2026-08-04: durable finalization and merge receipts drive explicit `merged`, `ready`, `no_changes`, `dirty`, `unfinalized`, and `blocked` states. Real API/git-worktree proof exercised every state and rejected non-ready merges; rendered UI proof confirms a zero-ahead no-change run says “No changes,” never “Merged,” and has no merge action. Regression/static evidence: 21 backend and 15 UI tests; UI lint/typecheck, Ruff/format, and full Pyright clean. | validated | None. |
| R6 | JSONL rotations never collide or overwrite and remain restart-safe. | Perform repeated same-range and rapid rotations plus restart recovery; every event appears exactly once and archive paths are distinct. | Validated 2026-08-04: archive naming now includes a content hash plus collision ordinal under atomic hard-link reservation; real temporary-file collision/restart/bootstrap proof discovered legacy and new segments and restored positions `[0,1,2,3]` exactly once. Regression/static gate: 29 passed; Ruff, format, and Pyright clean. | validated | None. |
| R7 | No active repository integration discovers or mandates Superpowers. | Run repository searches and active configuration checks proving no loader, shim, hook, or guidance depends on vendored Superpowers; preserve historical evidence where inert. | Validated 2026-08-04: removed active `.superpowers` residue; independent scoped scan confirmed no `opencode.json`, `.agents` loader, vendor child, hook, symlink, or guidance remains. The only remaining references are inert historical/remediation research documents. | validated | None. |
| R8 | Aggregate remediation has no remaining functional gaps and passes every gate. | Fresh independent gap review; product-real proofs above; full backend/UI suites, Ruff, format, Pyright, graph boundary, signal routing, removed-integration search, and final change review all clean. | Validated 2026-08-05 after repeated independent gap-finder, builder, and validator cycles. Final aggregate gates: backend 5,807 passed/4 documented skips; UI 476 passed; Ruff clean; format clean across 722 Python files; Pyright 0 errors/warnings; UI typecheck/lint clean; graph-projection boundary and non-vacuous signal routing clean; active-checkout Superpowers and retired-symbol scans empty. Final source audit confirmed bounded keyset reads, scalar outbox reset and artifact-GC reference probes, capped/truncated recovery reporting, no public GET replay, and correct expected-position 409/retryable 503 handling. Follow-up simplification removed the entire schema-upgrade runtime, 58 revision files, upgrade-only tests, direct dependency, stale codemod exemptions, and executable guidance. Independent product proof created a real fresh CLI database with exactly the 24 current ORM tables, no migration bookkeeping table, the required archival/fact columns and blocker-region index, and preserved data across repeated initialization; 26 focused graph paths and all 25 active routines passed. The final independent validator found no functional gap. | validated | None. |
