id: ACT-7
title: Start run through REST
definition: Accept a legacy start signal without claiming it has applied.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-2
actor: caller
permission_requirements:
- PER-1
- PER-2
- PER-3
preconditions:
- legacy run is draft
expected_source_version: None
required_input:
  run_id: required
validation:
- lifecycle validation
durable_effect: Appends SignalEnqueued(RUN_START); consumer later applies start.
resulting_state_id: STA-1
failure_modes:
- 409 illegal transition
stale_state_behavior: 202 may leave run draft; stale redelivery is discarded or idempotently
  handled.
idempotency: State-validated; no request key.
retry_behavior: Poll signal/activity before retry.
reversibility: Pause or cancel are separate actions.
audit_evidence_ids:
- EVD-7
- EVD-38
- EVD-39
- EVD-40
transition:
  from_state_id: STA-1
  to_state_id: STA-1
enforced_authentication: Optional global valid-bearer gate; disabled by default.
enforced_authorization: absent
domain_eligibility: Legacy row must be draft.
proposed_role_policy: null
conflict_ids:
- CON-6
question_ids:
- Q-5
test_status: exercised
documentation_status: conflicting
epistemic_status: observed
status_basis:
  implementation: 'ACT-7 implementation status present: ACT-7 Start run through REST is present: SER rows
    SER-0122 inspect src/orchestrator/api/routers/runs.py::start_run and establish the record proposition
    under EVD-7, EVD-38, EVD-39, EVD-40.'
  test: 'ACT-7 test status exercised: ACT-7 Start run through REST is exercised because admitted SER rows
    SER-0123 contain direct assertions at tests/integration/test_signal_queue.py::test_run_start_signal_consumed_draft_to_active;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-7 documentation status conflicting: ACT-7 Start run through REST is conflicting
    because reciprocal issue authority CON-6 (unresolved: Lifecycle REST acceptance differs from direct
    CLI application); Q-5 (open: Define product-role authorization before treating actor text as authority)
    names ACT-7 and remains unresolved; the conflict is supported by EVD-7, EVD-38, EVD-39, EVD-40, not
    prose alone.'
  capability: 'ACT-7 capability status unknown: ACT-7 Start run through REST remains unknown as a user
    capability: no CAP implementation_carrier_binding; EVD-7, EVD-38, EVD-39, EVD-40 establishes the record
    carrier but no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-7 epistemic status observed: ACT-7 Start run through REST is observed from direct/audit
    authority EVD-7, EVD-38, EVD-39, EVD-40 (EVD-7 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#state-carriers-and-legal-transitions);
    EVD-38 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#authority-model);
    EVD-39 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#authority-model);
    EVD-40 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#legacy-versus-graph-mode)),
    qualified by conflicts/questions [''CON-6'', ''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/runs.py::start_run
test_locators:
- tests/integration/test_signal_queue.py::test_run_start_signal_consumed_draft_to_active
bounded_test_locators: []
