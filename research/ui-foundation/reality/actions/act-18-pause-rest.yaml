id: ACT-18
title: Pause run through REST
definition: Accept a REST pause request by visibly moving the legacy row from active
  to stopping; consumer application to paused is later and separate.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-4
actor: caller
permission_requirements:
- PER-1
- PER-2
- PER-3
preconditions:
- legacy run is active
expected_source_version: No client run version.
required_input:
  run_id: required
validation:
- legacy lifecycle state validation
- active executor cancellation before pause enqueue
durable_effect: Synchronously persists active to stopping, appends pause signal acceptance
  evidence, and queues PAUSE; the consumer later applies stopping to paused.
resulting_state_id: STA-3
failure_modes:
- 404 missing run
- 409 stopping or illegal transition
- executor cancellation failure
stale_state_behavior: A concurrent lifecycle change can make the request reject; after
  acceptance, redelivery handles stale or already-applied PAUSE without a typed result
  receipt.
idempotency: State-validated; no idempotency key. A second pause from stopping is
  rejected.
retry_behavior: Refresh row status and activity before retrying; do not infer paused
  until the consumer applies the queued signal.
reversibility: Resume is a separate action after paused; it does not restore work
  interrupted by executor cancellation.
audit_evidence_ids:
- EVD-7
- EVD-8
- EVD-38
- EVD-39
- EVD-40
transition:
  from_state_id: STA-2
  to_state_id: STA-3
eventual_applied_transition:
  from_state_id: STA-3
  to_state_id: STA-4
  evidence_ids:
  - EVD-7
  - EVD-8
enforced_authentication: Optional global valid-bearer gate; disabled by default.
enforced_authorization: absent
domain_eligibility: Legacy row must be active; stopping is rejected.
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-18 implementation status present: ACT-18 Pause run through REST is present: SER
    rows SER-0015 inspect src/orchestrator/api/routers/runs.py::pause_run and establish the record proposition
    under EVD-7, EVD-8, EVD-38, EVD-39, EVD-40.'
  test: 'ACT-18 test status exercised: ACT-18 Pause run through REST is exercised because admitted SER
    rows SER-0016 contain direct assertions at tests/integration/test_signal_queue.py::test_pause_active_run_enters_stopping_then_paused;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-18 documentation status documented: ACT-18 Pause run through REST is documented
    at reality/actions/act-18-pause-rest.yaml and by EVD-7, EVD-8, EVD-38, EVD-39, EVD-40; any listed
    question (Q-5 (open: Define product-role authorization before treating actor text as authority)) is
    explicitly disclosed and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-18 capability status unknown: ACT-18 Pause run through REST remains unknown as a user
    capability: CAP-21=gap as reversibility-action; CAP-39=unknown as action-contract; CAP-40=unknown
    as action-contract; CAP-44=gap as reversibility-action; CAP-61=unknown as action-contract; CAP-89=unknown
    as action-contract; EVD-7, EVD-8, EVD-38, EVD-39, EVD-40 establishes the record carrier but no relevant
    CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-18 epistemic status observed: ACT-18 Pause run through REST is observed from direct/audit
    authority EVD-7, EVD-8, EVD-38, EVD-39, EVD-40 (EVD-7 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#state-carriers-and-legal-transitions);
    EVD-8 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#state-carriers-and-legal-transitions);
    EVD-38 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#authority-model);
    EVD-39 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#authority-model);
    EVD-40 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#legacy-versus-graph-mode)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/runs.py::pause_run
test_locators:
- tests/integration/test_signal_queue.py::test_pause_active_run_enters_stopping_then_paused
bounded_test_locators: []
