id: ACT-73
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-43
actor: REST caller
permission_requirements: [PER-1, PER-3]
preconditions: [legacy execution mode, task building, submission checks pass]
expected_source_version: 'None'
required_input: {run_id: required, task_id: required}
validation: [submission-gates, task-transition]
durable_effect: Validates and persists submission-gate side effects, then enqueues ACTIVITY_COMPLETED and commits its outbox; consumer application is separate.
resulting_state_id: STA-9
failure_modes: ['404', '409', '422', worktree commit error]
stale_state_behavior: Already-verifying submission does not enqueue another signal.
idempotency: Idempotent once verification is already pending.
retry_behavior: Correct gates and retry from current task state.
reversibility: No direct undo of accepted signal.
audit_evidence_ids: [EVD-110]
transition: {from_state_id: STA-9, to_state_id: STA-9}
carrier_relationship:
  affected_carrier: legacy task BUILDING status plus durable signal acceptance
  caveat: This carrier self-loop is non-lifecycle; BUILDING to VERIFYING occurs only when the signal consumer applies ACTIVITY_COMPLETED.
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-3 building task and satisfied gates
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-73 implementation status present: ACT-73 Validates and persists submission-gate
    side effects, then enqueues ACTIVITY_COMPLETED and commits its outbox; consumer application is separate.
    is present: SER rows SER-0463 inspect src/orchestrator/api/routers/tasks.py::submit_task and establish
    the record proposition under EVD-110.'
  test: 'ACT-73 test status exercised: ACT-73 Validates and persists submission-gate side effects, then
    enqueues ACTIVITY_COMPLETED and commits its outbox; consumer application is separate. is exercised
    because admitted SER rows SER-0483 contain direct assertions at tests/integration/test_idempotency.py::test_submit_twice;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-73 documentation status documented: ACT-73 Validates and persists submission-gate
    side effects, then enqueues ACTIVITY_COMPLETED and commits its outbox; consumer application is separate.
    is documented at reality/actions/act-73-task-submit-rest.yaml and by EVD-110; any listed question
    (Q-5 (open: Define product-role authorization before treating actor text as authority)) is explicitly
    disclosed and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-73 capability status unknown: ACT-73 Validates and persists submission-gate side effects,
    then enqueues ACTIVITY_COMPLETED and commits its outbox; consumer application is separate. remains
    unknown as a user capability: no CAP implementation_carrier_binding; EVD-110 establishes the record
    carrier but no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-73 epistemic status observed: ACT-73 Validates and persists submission-gate side effects,
    then enqueues ACTIVITY_COMPLETED and commits its outbox; consumer application is separate. is observed
    from direct/audit authority EVD-110 (EVD-110 (test: tests/integration/test_idempotency.py::test_submit_twice)), qualified
    by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators: [src/orchestrator/api/routers/tasks.py::submit_task]
test_locators:
- tests/integration/test_idempotency.py::test_submit_twice
bounded_test_locators: []
