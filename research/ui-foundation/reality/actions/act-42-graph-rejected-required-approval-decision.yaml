id: ACT-42
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-11
actor: Caller-supplied decider; persisted attribution is not authorization.
permission_requirements:
- PER-1
- PER-2
- PER-4
preconditions:
- A graph exists and is not cancelled or failed.
- The target is an eligible nonterminal required approval gate.
- The request value is rejected.
expected_source_version: No client graph position; current-position controller/append
  checks expose races as 409.
required_input:
  decision_type: approval
  value: rejected
  target_node_id: required
  decider: required
  scope: optional
  expiry: optional
  reason: optional
  record_id: optional
command_variant:
  discriminator: decision_type
  decision_type: approval
  value: rejected
validation:
- Strict RecordDecisionCommand approval type/value/target validation
- PER-4 graph state, terminality, and actor/context validation
durable_effect: Appends approval_decision_recorded and projects the rejected required
  approval as a DecisionRecord before its separate node-failure consequence.
resulting_state_id: STA-37
failure_modes:
- 404 when no graph exists
- 409 for stale append or command rejection
- 422 for invalid approval input
stale_state_behavior: A race returns 409 without manufacturing a generic rejection
  carrier.
idempotency: No REST idempotency key; duplicate behavior is command/state dependent.
retry_behavior: Refresh the decision and node projections before any retry.
reversibility: No decision-reversal command exists.
audit_evidence_ids:
- EVD-26
- EVD-27
transition:
  from_state_id: STA-36
  to_state_id: STA-37
carrier_relationship:
  kind: same-command decision carrier
  paired_node_outcome_action_id: ACT-43
  node_transition:
    from_state_ids:
    - STA-71
    - STA-72
    - STA-29
    - STA-30
    - STA-31
    - STA-32
    to_state_id: STA-34
  ordering: One CMD-11 command records the rejected decision and separately projects
    required-node failure; this is not a STA-37-to-node edge.
enforced_authentication: PER-1 optional global valid-bearer gate.
enforced_authorization: absent
domain_eligibility: PER-4 required approval target, value, graph-state, and actor/context
  validation.
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-42 implementation status present: ACT-42 Appends approval_decision_recorded and
    projects the rejected required approval as a DecisionRecord before its separate node-failure consequence.
    is present: SER rows SER-0065 inspect src/orchestrator/graph/_commands.py::_apply_record_decision
    and establish the record proposition under EVD-26, EVD-27.'
  test: 'ACT-42 test status exercised: ACT-42 Appends approval_decision_recorded and projects the rejected
    required approval as a DecisionRecord before its separate node-failure consequence. is exercised because
    admitted SER rows SER-0066 contain direct assertions at tests/integration/test_graph_decisions_api.py::test_record_rejected_approval_is_durable_and_dead_inputs_successor;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-42 documentation status documented: ACT-42 Appends approval_decision_recorded and
    projects the rejected required approval as a DecisionRecord before its separate node-failure consequence.
    is documented at reality/actions/act-42-graph-rejected-required-approval-decision.yaml and by EVD-26,
    EVD-27; any listed question (Q-5 (open: Define product-role authorization before treating actor text
    as authority)) is explicitly disclosed and does not create contradictory documentation for this bounded
    proposition.'
  capability: 'ACT-42 capability status unknown: ACT-42 Appends approval_decision_recorded and projects
    the rejected required approval as a DecisionRecord before its separate node-failure consequence. remains
    unknown as a user capability: no CAP implementation_carrier_binding; EVD-26, EVD-27 establishes the
    record carrier but no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-42 epistemic status observed: ACT-42 Appends approval_decision_recorded and projects
    the rejected required approval as a DecisionRecord before its separate node-failure consequence. is
    observed from direct/audit authority EVD-26, EVD-27 (EVD-26 (audit-report: research/ui-foundation/agent-reports/02-graph-runtime.md#command_specs-exhaustive-command-to-effect-table);
    EVD-27 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#graph-decision-patch-retiresupersede-and-requeue-capability)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/graph/_commands.py::_apply_record_decision
test_locators:
- tests/integration/test_graph_decisions_api.py::test_record_rejected_approval_is_durable_and_dead_inputs_successor
bounded_test_locators: []
