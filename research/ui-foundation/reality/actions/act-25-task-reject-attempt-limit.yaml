id: ACT-25
title: Reject legacy task at attempt limit
definition: Rejects one approval-pending legacy task when no revision attempt remains
  and fails that task.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-30
actor: fixed or injected user
permission_requirements:
- PER-3
preconditions:
- legacy task is pending user action for approval
- current attempt has reached the attempt limit
expected_source_version: No client task version
required_input:
  run_id: required
  task_id: required
  reason: optional
validation:
- legacy execution mode
- task rejection transition validation
- attempt limit evaluation
durable_effect: Records a rejected ApprovalDecision and changes the task from pending
  user action to failed.
resulting_state_id: STA-15
failure_modes:
- 404 run or task identity
- 409 task is not awaiting approval
stale_state_behavior: An advanced task is rejected by the task transition check.
idempotency: No idempotency key; a failed task cannot be rejected again through this
  route.
retry_behavior: Use the separately qualified recovery, fan-out retry, or force-accept
  route when applicable.
reversibility: No direct reversal of the recorded rejection.
audit_evidence_ids:
- EVD-12
transition:
  from_state_id: STA-11
  to_state_id: STA-15
enforced_authentication: Optional global valid-bearer gate
enforced_authorization: absent
domain_eligibility: Legacy task approval state at its attempt limit.
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-25 implementation status present: ACT-25 Reject legacy task at attempt limit is
    present: SER rows SER-0030 inspect src/orchestrator/api/routers/tasks.py::reject_task and establish
    the record proposition under EVD-12.'
  test: 'ACT-25 test status exercised: ACT-25 Reject legacy task at attempt limit is exercised because
    admitted SER rows SER-0031 contain direct assertions at tests/integration/test_approval_workflow.py::test_reject_at_max_attempts_transitions_to_failed_and_replays;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-25 documentation status documented: ACT-25 Reject legacy task at attempt limit is
    documented at reality/actions/act-25-task-reject-attempt-limit.yaml and by EVD-12; any listed question
    (Q-5 (open: Define product-role authorization before treating actor text as authority)) is explicitly
    disclosed and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-25 capability status unknown: ACT-25 Reject legacy task at attempt limit remains unknown
    as a user capability: CAP-27=unknown as action-contract; EVD-12 establishes the record carrier but
    no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-25 epistemic status observed: ACT-25 Reject legacy task at attempt limit is observed
    from direct/audit authority EVD-12 (EVD-12 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#state-carriers-and-legal-transitions)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/tasks.py::reject_task
test_locators:
- tests/integration/test_approval_workflow.py::test_reject_at_max_attempts_transitions_to_failed_and_replays
bounded_test_locators: []
