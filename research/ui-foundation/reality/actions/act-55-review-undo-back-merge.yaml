id: ACT-55
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-17
actor: caller
permission_requirements:
- PER-1
- PER-7
preconditions:
- git operation ready
- HEAD qualifying merge
expected_source_version: None
required_input:
  run_id: required
validation:
- worktree
- HEAD merge
durable_effect: Synchronously creates qualifying merge revert and emits BackMergeReverted.
resulting_state_id: STA-42
failure_modes:
- '409'
- git error
stale_state_behavior: HEAD may change.
idempotency: Not after HEAD change.
retry_behavior: Inspect HEAD.
reversibility: Compensates qualifying merge only.
audit_evidence_ids:
- EVD-29
transition:
  from_state_id: STA-59
  to_state_id: STA-42
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-7 git prerequisite
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: ACT-55 creates a qualifying merge revert and evidence.
  test: The real-repo route test reverts a clean back merge and asserts success.
  documentation: ACT-55 compensation is documented as a revert, not exact undo of all effects.
  capability: One revert path does not prove universal reversibility.
  epistemic: ACT-55 is observed by EVD-29.
implementation_locators:
- research/ui-foundation/reality/actions/act-55-review-undo-back-merge.yaml::durable_effect
- src/orchestrator/git/ops/branch_ops.py::revert_back_merge
test_locators:
- tests/integration/test_conflict_api.py::TestRevertBackMerge::test_revert_clean_back_merge_succeeds
