id: ACT-960
implementation_status: present
capability_status: current
executable: true
command_id: CMD-912
actor: "Caller-supplied decider recorded by ACT-910; no authenticated-subject authorization is enforced."
permission_requirements: [PER-900, PER-901, PER-903]
preconditions: ["ACT-910's eligible approved approval decision has been recorded.", "The target approval gate remains eligible for the command's accepted completion effect."]
expected_source_version: "This is the node-projection consequence of the same current-position decision command; no client graph position exists."
required_input: {decision_type: approval, value: approved, target_node_id: required}
validation: ["The preceding RecordDecisionCommand approval target/value and graph-state validation succeeds.", "PER-903 applies the command's target and state eligibility checks."]
durable_effect: "The accepted approved decision binds its DecisionRecord where applicable, releases the scoped lease where present, and projects the approval-gate node completed."
resulting_state_id: STA-929
failure_modes: ["409 when the decision command is rejected or loses its append race", "422 when the underlying approval payload is invalid"]
stale_state_behavior: "No independent client version applies; a decision-command conflict prevents this node outcome rather than moving a row carrier."
idempotency: "No separate idempotency key exists for this data-dependent command consequence."
retry_behavior: "Inspect the decision and node projections; do not independently replay the node outcome."
reversibility: "No decision reversal or node-completion undo is established."
audit_evidence_ids: [EVI-920]
transition: {from_state_id: STA-933, to_state_id: STA-929}
enforced_authentication: "PER-900 optional global valid-bearer gate."
enforced_authorization: absent
domain_eligibility: "PER-903 accepted approval-decision and target-node eligibility."
proposed_role_policy: null
