id: ACT-39
title: Reset run-recovery target task to building
definition: Run recovery moves its target task to building with a new attempt and
  expanded attempt budget.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-35
actor: caller
permission_requirements:
- PER-3
preconditions:
- legacy run is failed or paused
- target task is selected for recovery
expected_source_version: No client run or task version
required_input:
  run_id: required
  target_task_id: required
  additional_attempts: optional
  guidance: optional
  reset_branch: optional
validation:
- legacy execution mode
- failed-or-paused run validation
- target task validation
- additional attempt and reset strategy validation
durable_effect: Clears the target task's pending markers, appends a new attempt, expands
  its budget, and sets it to building.
resulting_state_id: STA-9
failure_modes:
- 404 run or target task identity
- 409 run is neither failed nor paused
- 409 worktree reset failure
stale_state_behavior: No expected version; concurrent run or task changes can alter
  recovery input state.
idempotency: No idempotency key; each successful recovery appends a target-task attempt.
retry_behavior: Refresh run, target task, and worktree state before retrying.
reversibility: Recovery rewind does not restore the target's former attempt history
  or file effects.
audit_evidence_ids:
- EVD-45
transition:
  from_state_ids:
  - STA-8
  - STA-15
  to_state_id: STA-9
carrier_relationship:
  command_group: run_recover
  paired_effect_action_ids:
  - ACT-38
  - ACT-40
  affected_carrier: recovery target legacy task
enforced_authentication: Optional global valid-bearer gate
enforced_authorization: absent
domain_eligibility: Selected target task of a failed or paused legacy run.
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: unexercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-39 implementation status present: ACT-39 Reset run-recovery target task to building
    is present: SER rows SER-0058 inspect src/orchestrator/workflow/service.py::WorkflowService.recover_run
    and establish the record proposition under EVD-45.'
  test: 'ACT-39 test status unexercised: ACT-39 Reset run-recovery target task to building remains unexercised:
    bounded SER rows SER-0059 cover tests/integration/test_api_runs_recover.py::test_recover_happy_path_sets_paused_and_rewinds
    but do not assert the complete record proposition.'
  documentation: 'ACT-39 documentation status documented: ACT-39 Reset run-recovery target task to building
    is documented at reality/actions/act-39-run-recover-target-building.yaml and by EVD-45; any listed
    question (no canonical unresolved conflict or question) is explicitly disclosed and does not create
    contradictory documentation for this bounded proposition.'
  capability: 'ACT-39 capability status unknown: ACT-39 Reset run-recovery target task to building remains
    unknown as a user capability: CAP-88=unknown as action-contract; EVD-45 establishes the record carrier
    but no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-39 epistemic status observed: ACT-39 Reset run-recovery target task to building is observed
    from direct/audit authority EVD-45 (EVD-45 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#run-and-lifecycle-capability)),
    with no canonical conflict/question qualification; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/workflow/service.py::WorkflowService.recover_run
test_locators: []
bounded_test_locators:
- tests/integration/test_api_runs_recover.py::test_recover_happy_path_sets_paused_and_rewinds
