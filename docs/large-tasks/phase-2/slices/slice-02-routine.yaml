routine:
  id: phase2-oversight-slice-02
  name: Phase 2 Oversight slice-02
  description: Deterministic smoke slice for the external oversight loop.
  steps:
  - id: S-01
    title: Execute slice-02
    step_context: This is a bounded oversight-loop smoke slice. It must only produce
      evidence for the current slice.
    tasks:
    - id: T-01
      title: Write slice-02 evidence
      script: 'mkdir -p docs/phase2-oversight-smoke && printf ''%s\n'' ''slice-02:
        Slice 02 changes focus from first-run execution to evidence-driven replanning.''
        ''assumption: The coordinator can replan after inspecting concrete slice evidence.''
        > docs/phase2-oversight-smoke/slice-02-evidence.txt'
      requirements:
      - id: R1
        desc: The coordinator can replan after inspecting concrete slice evidence.
      - id: R2
        desc: Slice 02 changes focus from first-run execution to evidence-driven replanning.
