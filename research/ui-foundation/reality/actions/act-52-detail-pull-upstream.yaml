id: ACT-52
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-15
actor: caller
permission_requirements:
- PER-1
- PER-7
preconditions:
- eligible active/paused run
- worktree/source branch
expected_source_version: None
required_input:
  run_id: required
validation:
- run/branch readiness
durable_effect: Synchronously clean-merges source into run branch.
resulting_state_id: STA-42
failure_modes:
- 409/400
- conflict is ACT-4
- git error
stale_state_behavior: No branch version.
idempotency: Git dependent.
retry_behavior: Inspect branch/conflicts.
reversibility: Qualified HEAD revert only.
audit_evidence_ids:
- EVD-29
transition:
  from_state_id: STA-59
  to_state_id: STA-42
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-7 git prerequisites
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-52 implementation status present: ACT-52 Synchronously clean-merges source into
    run branch. is present: SER rows SER-0086 inspect src/orchestrator/api/routers/runs.py::back_merge_endpoint
    and establish the record proposition under EVD-29.'
  test: 'ACT-52 test status exercised: ACT-52 Synchronously clean-merges source into run branch. is exercised
    because admitted SER rows SER-0087 contain direct assertions at tests/integration/test_api_branch_ops.py::test_back_merge;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-52 documentation status documented: ACT-52 Synchronously clean-merges source into
    run branch. is documented at reality/actions/act-52-detail-pull-upstream.yaml and by EVD-29; any listed
    question (Q-5 (open: Define product-role authorization before treating actor text as authority)) is
    explicitly disclosed and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-52 capability status unknown: ACT-52 Synchronously clean-merges source into run branch.
    remains unknown as a user capability: CAP-21=gap as reversibility-action; CAP-44=gap as reversibility-action;
    EVD-29 establishes the record carrier but no relevant CAP authority classifies this record as current
    or derived.'
  epistemic: 'ACT-52 epistemic status observed: ACT-52 Synchronously clean-merges source into run branch.
    is observed from direct/audit authority EVD-29 (EVD-29 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#repository-review-sourceconfiguration-and-file-effects)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/runs.py::back_merge_endpoint
test_locators:
- tests/integration/test_api_branch_ops.py::test_back_merge
bounded_test_locators: []
