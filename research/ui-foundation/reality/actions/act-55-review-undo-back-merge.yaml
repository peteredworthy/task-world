id: ACT-55
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-17
actor: caller
permission_requirements:
- PER-1
- PER-7
preconditions:
- git operation ready
- HEAD qualifying merge
expected_source_version: None
required_input:
  run_id: required
validation:
- worktree
- HEAD merge
durable_effect: Synchronously creates qualifying merge revert and emits BackMergeReverted.
resulting_state_id: STA-42
failure_modes:
- '409'
- git error
stale_state_behavior: HEAD may change.
idempotency: Not after HEAD change.
retry_behavior: Inspect HEAD.
reversibility: Compensates qualifying merge only.
audit_evidence_ids:
- EVD-29
transition:
  from_state_id: STA-59
  to_state_id: STA-42
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-7 git prerequisite
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: unexercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-55 implementation status present: ACT-55 Synchronously creates qualifying merge
    revert and emits BackMergeReverted. is present: SER rows SER-0092 inspect src/orchestrator/git/ops/branch_ops.py::revert_back_merge
    and establish the record proposition under EVD-29.'
  test: 'ACT-55 test status unexercised: ACT-55 Synchronously creates qualifying merge revert and emits
    BackMergeReverted. remains unexercised: bounded SER rows SER-0093 cover tests/integration/test_conflict_api.py::TestRevertBackMerge::test_revert_clean_back_merge_resets_head
    but do not assert the complete record proposition.'
  documentation: 'ACT-55 documentation status documented: ACT-55 Synchronously creates qualifying merge
    revert and emits BackMergeReverted. is documented at reality/actions/act-55-review-undo-back-merge.yaml
    and by EVD-29; any listed question (Q-5 (open: Define product-role authorization before treating actor
    text as authority)) is explicitly disclosed and does not create contradictory documentation for this
    bounded proposition.'
  capability: 'ACT-55 capability status unknown: ACT-55 Synchronously creates qualifying merge revert
    and emits BackMergeReverted. remains unknown as a user capability: CAP-21=gap as reversibility-action;
    CAP-44=gap as reversibility-action; EVD-29 establishes the record carrier but no relevant CAP authority
    classifies this record as current or derived.'
  epistemic: 'ACT-55 epistemic status observed: ACT-55 Synchronously creates qualifying merge revert and
    emits BackMergeReverted. is observed from direct/audit authority EVD-29 (EVD-29 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#repository-review-sourceconfiguration-and-file-effects)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/git/ops/branch_ops.py::revert_back_merge
test_locators: []
bounded_test_locators:
- tests/integration/test_conflict_api.py::TestRevertBackMerge::test_revert_clean_back_merge_resets_head
