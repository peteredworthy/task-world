id: ACT-76
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-46
actor: REST caller
permission_requirements: [PER-1, PER-3]
preconditions: [legacy execution mode, active run, verifying or terminal task]
expected_source_version: 'None'
required_input: {run_id: required, task_id: required, req_id: required, grade: required, grade_reason: optional}
validation: [grade, task-eligibility]
durable_effect: Persists the requirement grade and reason through the workflow service.
resulting_state_id: STA-10
failure_modes: ['404', '409', validation error]
stale_state_behavior: Explicitly rejects PENDING (STA-8), BUILDING (STA-9), PENDING_USER_ACTION (STA-11), RECOVERING (STA-12), and FAN_OUT_RUNNING (STA-13).
idempotency: Repeating the same grade converges.
retry_behavior: Re-read requirement before retrying.
reversibility: Later eligible grade update supersedes it.
audit_evidence_ids: [EVD-121, EVD-122, EVD-138]
transition_variants:
- {carrier: task, from_state_id: STA-10, to_state_id: STA-10, eligibility_precondition: 'active run and task status is exactly VERIFYING', effect_kind: self-loop-field-mutation, evidence_ids: [EVD-138]}
- {carrier: task, from_state_id: STA-14, to_state_id: STA-14, eligibility_precondition: 'active run and task status is exactly COMPLETED', effect_kind: self-loop-field-mutation, evidence_ids: [EVD-138]}
- {carrier: task, from_state_id: STA-15, to_state_id: STA-15, eligibility_precondition: 'active run and task status is exactly FAILED', effect_kind: self-loop-field-mutation, evidence_ids: [EVD-138]}
carrier_relationship:
  affected_carrier: checklist item grade and grade_reason while the representative VERIFYING task status is preserved
  caveat: This is a field-mutation carrier self-loop, not a task lifecycle transition; the handler also tolerates terminal task carriers.
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-3 verifier-state eligibility
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-76 implementation status present: ACT-76 Persists the requirement grade and reason
    through the workflow service across exact accepted and rejected TaskStatus carriers. is present: SER rows SER-0466, SER-0509 inspect the REST route and WorkflowService.set_grade
    under EVD-121, EVD-138.'
  test: 'ACT-76 test status exercised: ACT-76 Persists the requirement grade and reason through the workflow
    service. is exercised because admitted SER rows SER-0486 contain direct assertions at tests/integration/test_api_tasks.py::test_full_task_lifecycle;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-76 documentation status documented: ACT-76 Persists the requirement grade and reason
    through the workflow service. is documented at reality/actions/act-76-grade-update-rest.yaml and by
    EVD-121, EVD-122, EVD-138; any listed question (Q-5 (open: Define product-role authorization before treating actor text
    as authority)) is explicitly disclosed and does not create contradictory documentation for this bounded
    proposition.'
  capability: 'ACT-76 capability status unknown: ACT-76 Persists the requirement grade and reason through
    the workflow service. remains unknown as a user capability: no CAP implementation_carrier_binding;
    EVD-121, EVD-122, EVD-138 establish the record carrier but no relevant CAP authority classifies this record as current
    or derived.'
  epistemic: 'ACT-76 epistemic status observed: ACT-76 Persists the requirement grade and reason through
    the workflow service. is observed from direct implementation/test authority EVD-121, EVD-122, EVD-138,
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/tasks.py::set_grade
- src/orchestrator/workflow/service.py::WorkflowService.set_grade
test_locators:
- tests/integration/test_api_tasks.py::test_full_task_lifecycle
bounded_test_locators: []
