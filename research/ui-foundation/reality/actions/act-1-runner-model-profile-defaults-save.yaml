id: ACT-1
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-23
actor: caller
permission_requirements:
- PER-1
- PER-7
preconditions:
- selectable runner
- body/path type match
expected_source_version: None
required_input:
  runner_type: required
  model_profile_defaults: required
validation:
- runner type
- mapping
durable_effect: Synchronously replaces runner default rows for future resolution.
resulting_state_id: STA-49
failure_modes:
- '422'
- database error
stale_state_behavior: No version fence.
idempotency: Identical replacement converges.
retry_behavior: Reload defaults.
reversibility: Later save replaces values.
audit_evidence_ids:
- EVD-34
transition:
  from_state_id: STA-49
  to_state_id: STA-49
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-7 runner-default prerequisites
proposed_role_policy: null
conflict_ids: []
question_ids:
- Q-5
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-1 implementation status present: ACT-1 Synchronously replaces runner default rows
    for future resolution. is present: SER rows SER-0001 inspect src/orchestrator/api/routers/runners.py::set_agent_runner_model_profile_defaults
    and establish the record proposition under EVD-34.'
  test: 'ACT-1 test status exercised: ACT-1 Synchronously replaces runner default rows for future resolution.
    is exercised because admitted SER rows SER-0002 contain direct assertions at tests/integration/test_api_model_profiles.py::TestAgentRunnerModelDefaultEndpoints::test_put_replaces_all_existing_model_defaults;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-1 documentation status documented: ACT-1 Synchronously replaces runner default rows
    for future resolution. is documented at reality/actions/act-1-runner-model-profile-defaults-save.yaml
    and by EVD-34; any listed question (Q-5 (open: Define product-role authorization before treating actor
    text as authority)) is explicitly disclosed and does not create contradictory documentation for this
    bounded proposition.'
  capability: 'ACT-1 capability status unknown: ACT-1 Synchronously replaces runner default rows for future
    resolution. remains unknown as a user capability: no CAP implementation_carrier_binding; EVD-34 establishes
    the record carrier but no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-1 epistemic status observed: ACT-1 Synchronously replaces runner default rows for future
    resolution. is observed from direct/audit authority EVD-34 (EVD-34 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#repository-review-sourceconfiguration-and-file-effects)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/runners.py::set_agent_runner_model_profile_defaults
test_locators:
- tests/integration/test_api_model_profiles.py::TestAgentRunnerModelDefaultEndpoints::test_put_replaces_all_existing_model_defaults
bounded_test_locators: []
