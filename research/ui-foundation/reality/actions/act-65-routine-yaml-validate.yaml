id: ACT-65
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-26
actor: caller
permission_requirements:
- PER-1
- PER-6
preconditions:
- yaml_content supplied
expected_source_version: None; validation saves no routine
required_input:
  yaml_content: required
validation:
- YAML parse
- optional routine unwrap
- RoutineConfig schema
durable_effect: Synchronously returns a valid or invalid response without writing
  routine source or metadata.
resulting_state_id: STA-61
failure_modes:
- invalid YAML
- schema validation response
stale_state_behavior: No source version because the response is non-mutating.
idempotency: Pure validation for identical content.
retry_behavior: Correct content and resubmit.
reversibility: Non-mutating; no undo.
audit_evidence_ids:
- EVD-46
transition:
  from_state_id: STA-61
  to_state_id: STA-61
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-6 parse/schema eligibility
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: unexercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-65 implementation status present: ACT-65 Synchronously returns a valid or invalid
    response without writing routine source or metadata. is present: SER rows SER-0111 inspect src/orchestrator/api/routers/routines.py::validate_routine
    and establish the record proposition under EVD-46.'
  test: 'ACT-65 test status unexercised: ACT-65 Synchronously returns a valid or invalid response without
    writing routine source or metadata. remains unexercised: bounded SER rows SER-0112, SER-0113 cover
    tests/integration/test_api_routines.py::test_validate_routine_invalid_missing_field, tests/integration/test_api_routines.py::test_validate_routine_valid
    but do not assert the complete record proposition.'
  documentation: 'ACT-65 documentation status documented: ACT-65 Synchronously returns a valid or invalid
    response without writing routine source or metadata. is documented at reality/actions/act-65-routine-yaml-validate.yaml
    and by EVD-46; any listed question (no canonical unresolved conflict or question) is explicitly disclosed
    and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-65 capability status unknown: ACT-65 Synchronously returns a valid or invalid response
    without writing routine source or metadata. remains unknown as a user capability: CAP-103=unknown
    as action-contract; EVD-46 establishes the record carrier but no relevant CAP authority classifies
    this record as current or derived.'
  epistemic: 'ACT-65 epistemic status observed: ACT-65 Synchronously returns a valid or invalid response
    without writing routine source or metadata. is observed from direct/audit authority EVD-46 (EVD-46
    (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#remaining-cli-surface-inventory)),
    with no canonical conflict/question qualification; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/routines.py::validate_routine
test_locators: []
bounded_test_locators:
- tests/integration/test_api_routines.py::test_validate_routine_invalid_missing_field
- tests/integration/test_api_routines.py::test_validate_routine_valid
