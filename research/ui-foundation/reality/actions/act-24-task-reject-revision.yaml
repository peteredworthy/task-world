id: ACT-24
title: Reject legacy task with revision attempts remaining
definition: Rejects one approval-pending legacy task when another revision attempt
  is available.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-30
actor: fixed or injected user
permission_requirements:
- PER-3
preconditions:
- legacy task is pending user action for approval
- current attempt is below the attempt limit
expected_source_version: No client task version
required_input:
  run_id: required
  task_id: required
  reason: optional
validation:
- legacy execution mode
- task rejection transition validation
- attempt limit evaluation
durable_effect: Records a rejected ApprovalDecision, creates the revision attempt,
  and returns the task to building.
resulting_state_id: STA-9
failure_modes:
- 404 run or task identity
- 409 task is not awaiting approval
stale_state_behavior: An advanced task is rejected by the task transition check.
idempotency: No idempotency key; a successful rejection changes state and creates
  a revision attempt.
retry_behavior: Refresh task state; retry is valid only while the task remains approval-pending.
reversibility: No direct reversal of the recorded rejection.
audit_evidence_ids:
- EVD-12
transition:
  from_state_id: STA-11
  to_state_id: STA-9
enforced_authentication: Optional global valid-bearer gate
enforced_authorization: absent
domain_eligibility: Legacy task approval state with an attempt remaining.
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-24 implementation status present: ACT-24 Reject legacy task with revision attempts
    remaining is present: SER rows SER-0028 inspect src/orchestrator/api/routers/tasks.py::reject_task
    and establish the record proposition under EVD-12.'
  test: 'ACT-24 test status exercised: ACT-24 Reject legacy task with revision attempts remaining is exercised
    because admitted SER rows SER-0029 contain direct assertions at tests/integration/test_approval_workflow.py::test_reject_task_transitions_to_building;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-24 documentation status documented: ACT-24 Reject legacy task with revision attempts
    remaining is documented at reality/actions/act-24-task-reject-revision.yaml and by EVD-12; any listed
    question (Q-5 (open: Define product-role authorization before treating actor text as authority)) is
    explicitly disclosed and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-24 capability status unknown: ACT-24 Reject legacy task with revision attempts remaining
    remains unknown as a user capability: CAP-21=gap as reversibility-action; CAP-27=unknown as action-contract;
    EVD-12 establishes the record carrier but no relevant CAP authority classifies this record as current
    or derived.'
  epistemic: 'ACT-24 epistemic status observed: ACT-24 Reject legacy task with revision attempts remaining
    is observed from direct/audit authority EVD-12 (EVD-12 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#state-carriers-and-legal-transitions)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/tasks.py::reject_task
test_locators:
- tests/integration/test_approval_workflow.py::test_reject_task_transitions_to_building
bounded_test_locators: []
