id: ACT-62
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-20
actor: caller
permission_requirements:
- PER-1
- PER-7
preconditions:
- preview/current git operation ready
- nonempty valid selection
expected_source_version: None
required_input:
  run_id: required
  files: required
validation:
- selection
- base SHA
durable_effect: Synchronously creates prune commit effects and emits PruneApplied; returned UUID is not
  durable event identity.
resulting_state_id: STA-42
failure_modes:
- 400 empty selection
- git error
stale_state_behavior: No version; preview can be stale.
idempotency: Retry can add commits.
retry_behavior: Inspect commits/worktree.
reversibility: No prune undo endpoint.
audit_evidence_ids:
- EVD-28
transition:
  from_state_id: STA-63
  to_state_id: STA-42
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-7 review prerequisites
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: ACT-62 applies selected prune effects and creates commit evidence.
  test: The real-repo test asserts a new prune commit is created.
  documentation: ACT-62 apply contract is documented.
  capability: One apply mode does not prove all prune safety.
  epistemic: ACT-62 is observed by EVD-28.
implementation_locators:
- research/ui-foundation/reality/actions/act-62-prune-apply.yaml::durable_effect
- src/orchestrator/git/ops/prune_ops.py::apply_prune
test_locators:
- tests/integration/test_prune_api.py::TestPruneApply::test_apply_creates_commit
