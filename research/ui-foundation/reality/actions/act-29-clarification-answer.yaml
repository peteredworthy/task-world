id: ACT-29
title: Answer legacy clarification
definition: Records non-skipped answers for a pending legacy clarification and returns
  the eligible task to building.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-8
actor: legacy responder
permission_requirements:
- PER-3
preconditions:
- identified clarification request belongs to the legacy task
- task is pending user action for clarification
expected_source_version: No client request version
required_input:
  run_id: required
  task_id: required
  request_id: required
  answers: required
validation:
- clarification response schema
- clarification task transition validation
durable_effect: Persists the response and config delta, appends the Q&A artifact,
  clears the clarification wait, and changes the task to building.
resulting_state_id: STA-9
failure_modes:
- 404 request or task identity
- 409 stale task/request relationship
- 422 malformed answer payload
stale_state_behavior: A stale response can clear its stale pending marker without
  reopening an already advanced task.
idempotency: No idempotency key; artifact content can duplicate before a database
  conflict settles.
retry_behavior: Refresh request and task state before retrying an uncertain response.
reversibility: No answer-revision or undo command.
audit_evidence_ids:
- EVD-13
transition:
  from_state_id: STA-11
  to_state_id: STA-9
enforced_authentication: Optional global valid-bearer gate
enforced_authorization: absent
domain_eligibility: Legacy clarification request and task wait state.
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-29 implementation status present: ACT-29 Answer legacy clarification is present:
    SER rows SER-0037 inspect src/orchestrator/workflow/service.py::WorkflowService.respond_to_clarification
    and establish the record proposition under EVD-13.'
  test: 'ACT-29 test status exercised: ACT-29 Answer legacy clarification is exercised because admitted
    SER rows SER-0038 contain direct assertions at tests/integration/test_clarification_workflow.py::test_full_clarification_cycle;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-29 documentation status documented: ACT-29 Answer legacy clarification is documented
    at reality/actions/act-29-clarification-answer.yaml and by EVD-13; any listed question (Q-5 (open:
    Define product-role authorization before treating actor text as authority)) is explicitly disclosed
    and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-29 capability status unknown: ACT-29 Answer legacy clarification remains unknown as
    a user capability: CAP-21=gap as reversibility-action; CAP-87=unknown as action-contract; CAP-107=unknown
    as action-contract; EVD-13 establishes the record carrier but no relevant CAP authority classifies
    this record as current or derived.'
  epistemic: 'ACT-29 epistemic status observed: ACT-29 Answer legacy clarification is observed from direct/audit
    authority EVD-13 (EVD-13 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#legacy-task-approval-clarification-and-retry-capability)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/workflow/service.py::WorkflowService.respond_to_clarification
test_locators:
- tests/integration/test_clarification_workflow.py::test_full_clarification_cycle
bounded_test_locators: []
