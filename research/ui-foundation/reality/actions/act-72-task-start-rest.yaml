id: ACT-72
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-42
actor: REST caller
permission_requirements: [PER-1, PER-3]
preconditions: [legacy execution mode, task pending]
expected_source_version: 'None'
required_input: {run_id: required, task_id: required}
validation: [legacy-mode, task-transition]
durable_effect: Starts the addressed legacy task through WorkflowService.start_task.
resulting_state_id: STA-9
failure_modes: ['404', '409', transition error]
stale_state_behavior: Concurrent state changes reject through transition validation.
idempotency: Not idempotent after task starts.
retry_behavior: Re-read task state before retrying.
reversibility: Use a separately eligible lifecycle/recovery command; no direct undo.
audit_evidence_ids: [EVD-117, EVD-118]
transition: {from_state_id: STA-8, to_state_id: STA-9}
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-3 legacy task state eligibility
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-72 implementation status present: ACT-72 Starts the addressed legacy task through
    WorkflowService.start_task. is present: SER rows SER-0462 inspect src/orchestrator/api/routers/tasks.py::start_task
    and establish the record proposition under EVD-117.'
  test: 'ACT-72 test status exercised: ACT-72 Starts the addressed legacy task through WorkflowService.start_task.
    is exercised because admitted SER rows SER-0482 contain direct assertions at tests/integration/test_api_tasks.py::test_full_task_lifecycle;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-72 documentation status documented: ACT-72 Starts the addressed legacy task through
    WorkflowService.start_task. is documented at reality/actions/act-72-task-start-rest.yaml and by EVD-117, EVD-118;
    any listed question (Q-5 (open: Define product-role authorization before treating actor text as authority))
    is explicitly disclosed and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-72 capability status unknown: ACT-72 Starts the addressed legacy task through WorkflowService.start_task.
    remains unknown as a user capability: no CAP implementation_carrier_binding; EVD-117, EVD-118 establish the
    record carrier but no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-72 epistemic status observed: ACT-72 Starts the addressed legacy task through WorkflowService.start_task.
    is observed from direct implementation/test authority EVD-117, EVD-118,
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators: [src/orchestrator/api/routers/tasks.py::start_task]
test_locators:
- tests/integration/test_api_tasks.py::test_full_task_lifecycle
bounded_test_locators: []
