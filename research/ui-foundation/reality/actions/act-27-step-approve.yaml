id: ACT-27
title: Record legacy step approval
definition: Records approval for the current legacy step's human-approval gate.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-32
actor: caller-supplied approver
permission_requirements:
- PER-3
preconditions:
- target is the current actionable step
- step has a human-approval gate
expected_source_version: No client step or run version
required_input:
  run_id: required
  step_id: required
  approved_by: required
  comment: optional
validation:
- current actionable-step validation
- human-approval-gate validation
durable_effect: Records StepHumanApprovalRecorded on the step-approval carrier; an eligible paused run
  may separately resume.
resulting_state_id: STA-53
failure_modes:
- 404 run or step identity
- 409 non-current step or absent human-approval gate
stale_state_behavior: A non-current or no-longer-gated step is rejected.
idempotency: Repeated approval is last-write-wins on the step-approval carrier.
retry_behavior: Refresh the pending-action view before retrying.
reversibility: No legacy step-deny, defer, or approval-reversal command.
audit_evidence_ids:
- EVD-36
transition:
  from_state_id: STA-53
  to_state_id: STA-53
enforced_authentication: Optional global valid-bearer gate
enforced_authorization: absent
domain_eligibility: Current legacy step with a human-approval gate.
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: unexercised
documentation_status: conflicting
epistemic_status: observed
status_basis:
  implementation: 'Implementation locator: research/ui-foundation/reality/actions/act-27-step-approve.yaml::durable_effect.
    Implementation locator: src/orchestrator/api/routers/runs.py::approve_step. ACT-27 records step approval
    and may separately permit resume.'
  test: No exact active-snapshot pytest symbol establishes this record's bounded proposition.
  documentation: ACT-27 conflicts on incomplete summary/comment guarantees.
  capability: The test does not prove enforced human authorization.
  epistemic: ACT-27 is observed by EVD-36.
