id: ACT-9
title: Delete run
definition: Synchronously delete an ordinary eligible legacy run, return the deleted-response
  carrier, and leave later row absence separately observable.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-7
actor: caller
permission_requirements:
- PER-1
- PER-2
- PER-3
preconditions:
- legacy run is draft, stopping, completed, failed, or cancelled
expected_source_version: None
required_input:
  run_id: required
validation:
- status-limited deletion
durable_effect: Deletes the eligible run row and owned records; the synchronous response
  confirms deletion, while later readback observes absence.
resulting_state_id: STA-58
failure_modes:
- 404 missing
- 409 active or paused
- 409 paused startup orphan requires ACT-22 qualification
stale_state_behavior: Concurrent start/pause can make deletion conflict.
idempotency: Repeat after deletion returns missing.
retry_behavior: Refresh status; do not retry destructive request blindly.
reversibility: Irreversible.
audit_evidence_ids:
- EVD-43
- EVD-38
- EVD-39
- EVD-40
transition:
  from_state_ids:
  - STA-1
  - STA-3
  - STA-5
  - STA-6
  - STA-7
  to_state_id: STA-58
later_absence_transition:
  from_state_id: STA-58
  to_state_id: STA-56
  evidence_ids:
  - EVD-43
enforced_authentication: Optional global valid-bearer gate; disabled by default.
enforced_authorization: absent
domain_eligibility: Ordinary route eligibility is draft, stopping, completed, failed,
  or cancelled. This contract does not delete an ordinary paused row; the qualified
  paused startup-orphan path is ACT-22.
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-9 implementation status present: ACT-9 Delete run is present: SER rows SER-0129
    inspect src/orchestrator/api/routers/runs.py::delete_run and establish the record proposition under
    EVD-43, EVD-38, EVD-39, EVD-40.'
  test: 'ACT-9 test status exercised: ACT-9 Delete run is exercised because admitted SER rows SER-0130
    contain direct assertions at tests/integration/test_api_runs.py::test_delete_run; no active rejected
    locator defeats that proof.'
  documentation: 'ACT-9 documentation status documented: ACT-9 Delete run is documented at reality/actions/act-9-delete-run.yaml
    and by EVD-43, EVD-38, EVD-39, EVD-40; any listed question (Q-5 (open: Define product-role authorization
    before treating actor text as authority)) is explicitly disclosed and does not create contradictory
    documentation for this bounded proposition.'
  capability: 'ACT-9 capability status unknown: ACT-9 Delete run remains unknown as a user capability:
    no CAP implementation_carrier_binding; EVD-43, EVD-38, EVD-39, EVD-40 establishes the record carrier
    but no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-9 epistemic status observed: ACT-9 Delete run is observed from direct/audit authority
    EVD-43, EVD-38, EVD-39, EVD-40 (EVD-43 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#run-and-lifecycle-capability);
    EVD-38 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#authority-model);
    EVD-39 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#authority-model);
    EVD-40 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#legacy-versus-graph-mode)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/runs.py::delete_run
test_locators:
- tests/integration/test_api_runs.py::test_delete_run
bounded_test_locators: []
