id: ACT-904
title: Delete run
definition: Synchronously delete an eligible legacy run, return the deleted-response carrier, and leave later row absence separately observable.
implementation_status: present
capability_status: current
executable: true
command_id: CMD-906
actor: caller
permission_requirements: [PER-900, PER-901, PER-902]
preconditions: ["run is not active or paused except narrow orphan case"]
expected_source_version: "None"
required_input: {run_id: required}
validation: ["status-limited deletion"]
durable_effect: "Deletes the eligible run row and owned records; the synchronous response confirms deletion, while later readback observes absence."
resulting_state_id: STA-956
failure_modes: ["404 missing", "409 active or paused"]
stale_state_behavior: "Concurrent start/pause can make deletion conflict."
idempotency: "Repeat after deletion returns missing."
retry_behavior: "Refresh status; do not retry destructive request blindly."
reversibility: "Irreversible."
audit_evidence_ids: [EVI-936, EVI-931, EVI-932, EVI-933]
transition: {from_state_ids: [STA-900, STA-902, STA-904, STA-905, STA-906], to_state_id: STA-956}
later_absence_transition: {from_state_id: STA-956, to_state_id: STA-954, evidence_ids: [EVI-936]}
enforced_authentication: "Optional global valid-bearer gate; disabled by default."
enforced_authorization: absent
domain_eligibility: "State-model-covered sources are draft, stopping, completed, failed, or cancelled; the route also has a narrow paused startup-orphan exception that lacks a declared deleted-response edge."
proposed_role_policy: null
