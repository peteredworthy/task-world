id: ACT-62
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-20
actor: caller
permission_requirements:
- PER-1
- PER-7
preconditions:
- preview/current git operation ready
- nonempty valid selection
expected_source_version: None
required_input:
  run_id: required
  files: required
validation:
- selection
- base SHA
durable_effect: Synchronously creates prune commit effects and emits PruneApplied;
  returned UUID is not durable event identity.
resulting_state_id: STA-42
failure_modes:
- 400 empty selection
- git error
stale_state_behavior: No version; preview can be stale.
idempotency: Retry can add commits.
retry_behavior: Inspect commits/worktree.
reversibility: No prune undo endpoint.
audit_evidence_ids:
- EVD-28
transition:
  from_state_id: STA-63
  to_state_id: STA-42
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-7 review prerequisites
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-62 implementation status present: ACT-62 Synchronously creates prune commit effects
    and emits PruneApplied; returned UUID is not durable event identity. is present: SER rows SER-0105
    inspect src/orchestrator/git/ops/prune_ops.py::apply_prune and establish the record proposition under
    EVD-28.'
  test: 'ACT-62 test status exercised: ACT-62 Synchronously creates prune commit effects and emits PruneApplied;
    returned UUID is not durable event identity. is exercised because admitted SER rows SER-0106 contain
    direct assertions at tests/integration/test_prune_api.py::TestPruneApply::test_apply_creates_commit;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-62 documentation status documented: ACT-62 Synchronously creates prune commit effects
    and emits PruneApplied; returned UUID is not durable event identity. is documented at reality/actions/act-62-prune-apply.yaml
    and by EVD-28; any listed question (Q-5 (open: Define product-role authorization before treating actor
    text as authority)) is explicitly disclosed and does not create contradictory documentation for this
    bounded proposition.'
  capability: 'ACT-62 capability status unknown: ACT-62 Synchronously creates prune commit effects and
    emits PruneApplied; returned UUID is not durable event identity. remains unknown as a user capability:
    no CAP implementation_carrier_binding; EVD-28 establishes the record carrier but no relevant CAP authority
    classifies this record as current or derived.'
  epistemic: 'ACT-62 epistemic status observed: ACT-62 Synchronously creates prune commit effects and
    emits PruneApplied; returned UUID is not durable event identity. is observed from direct/audit authority
    EVD-28 (EVD-28 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#repository-review-sourceconfiguration-and-file-effects)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/git/ops/prune_ops.py::apply_prune
test_locators:
- tests/integration/test_prune_api.py::TestPruneApply::test_apply_creates_commit
bounded_test_locators: []
