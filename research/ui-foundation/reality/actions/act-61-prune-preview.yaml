id: ACT-61
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-19
actor: caller
permission_requirements:
- PER-1
- PER-7
preconditions:
- worktree exists
- selection supplied
expected_source_version: None
required_input:
  run_id: required
  files: required
validation:
- selection
- base SHA
durable_effect: Synchronously returns non-mutating computed diff/count preview.
resulting_state_id: STA-63
failure_modes:
- git/base-SHA error
stale_state_behavior: Preview can be stale before apply.
idempotency: Pure for unchanged worktree.
retry_behavior: Recompute after changes.
reversibility: Non-mutating.
audit_evidence_ids:
- EVD-28
transition:
  from_state_id: STA-59
  to_state_id: STA-63
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-7 review prerequisites
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-61 implementation status present: ACT-61 Synchronously returns non-mutating computed
    diff/count preview. is present: SER rows SER-0103 inspect src/orchestrator/git/ops/prune_ops.py::preview_prune
    and establish the record proposition under EVD-28.'
  test: 'ACT-61 test status exercised: ACT-61 Synchronously returns non-mutating computed diff/count preview.
    is exercised because admitted SER rows SER-0104 contain direct assertions at tests/integration/test_prune_api.py::TestPrunePreview::test_preview_file_mode_lifecycle;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-61 documentation status documented: ACT-61 Synchronously returns non-mutating computed
    diff/count preview. is documented at reality/actions/act-61-prune-preview.yaml and by EVD-28; any
    listed question (no canonical unresolved conflict or question) is explicitly disclosed and does not
    create contradictory documentation for this bounded proposition.'
  capability: 'ACT-61 capability status unknown: ACT-61 Synchronously returns non-mutating computed diff/count
    preview. remains unknown as a user capability: no CAP implementation_carrier_binding; EVD-28 establishes
    the record carrier but no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-61 epistemic status observed: ACT-61 Synchronously returns non-mutating computed diff/count
    preview. is observed from direct/audit authority EVD-28 (EVD-28 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#repository-review-sourceconfiguration-and-file-effects)),
    with no canonical conflict/question qualification; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/git/ops/prune_ops.py::preview_prune
test_locators:
- tests/integration/test_prune_api.py::TestPrunePreview::test_preview_file_mode_lifecycle
bounded_test_locators: []
