id: ACT-79
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-49
actor: REST caller
permission_requirements:
- PER-1
- PER-7
preconditions:
- run worktree exists
- file path accepted
- base SHA resolves
expected_source_version: None
required_input:
  run_id: required
  file_path: required
validation:
- worktree
- file-path
- base-SHA
durable_effect: Reverts one worktree file to base state and creates a git commit.
resulting_state_id: STA-42
failure_modes:
- '404'
- '500'
- git error
stale_state_behavior: Base or worktree changes can change the revert result.
idempotency: Not idempotent after its created commit.
retry_behavior: Inspect returned commit and worktree diff.
reversibility: Revert the created git commit separately.
audit_evidence_ids:
- EVD-29
transition:
  from_state_id: STA-59
  to_state_id: STA-42
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-7 review worktree prerequisites
proposed_role_policy: null
conflict_ids: []
question_ids:
- Q-5
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-79 implementation status present: ACT-79 Reverts one worktree
    file to base state and creates a git commit. is present: SER rows SER-0469 inspect
    src/orchestrator/api/routers/review.py::revert_file_endpoint and establish the
    record proposition under EVD-29.'
  test: 'ACT-79 test status exercised: admitted SER row SER-0489 directly asserts exact
    base-content restoration and creation/readback of the route-created commit at
    tests/integration/test_prune_api.py::TestRevertFileEndpoint::test_revert_file_lifecycle.'
  documentation: 'ACT-79 documentation status documented: ACT-79 Reverts one worktree
    file to base state and creates a git commit. is documented at reality/actions/act-79-review-file-revert-rest.yaml
    and by EVD-29; any listed question (Q-5 (open: Define product-role authorization
    before treating actor text as authority)) is explicitly disclosed and does not
    create contradictory documentation for this bounded proposition.'
  capability: 'ACT-79 capability status unknown: ACT-79 Reverts one worktree file
    to base state and creates a git commit. remains unknown as a user capability:
    no CAP implementation_carrier_binding; EVD-29 establishes the record carrier but
    no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-79 epistemic status observed: ACT-79 Reverts one worktree file to
    base state and creates a git commit. is observed from direct/audit authority EVD-29
    (EVD-29 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#repository-review-sourceconfiguration-and-file-effects)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition
    is observed.'
implementation_locators:
- src/orchestrator/api/routers/review.py::revert_file_endpoint
test_locators:
- tests/integration/test_prune_api.py::TestRevertFileEndpoint::test_revert_file_lifecycle
bounded_test_locators: []
