id: ACT-36
title: Preserve legacy run carrier during transition-back
definition: Transition-back preserves the legacy run row carrier while it rewinds
  current-step position.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-34
actor: caller
permission_requirements:
- PER-3
preconditions:
- target step index is earlier than the current legacy step index
expected_source_version: No client run or step version
required_input:
  run_id: required
  target_step_index: required
  reason: optional
validation:
- legacy execution mode
- target step index bounds validation
- earlier-step validation
durable_effect: Records RunStepBackward and preserves the run carrier while changing
  current_step_index; paired task-reset effects are separate contracts.
resulting_state_id: STA-2
failure_modes:
- 404 run identity
- 409 target is current, forward, or out of bounds
stale_state_behavior: No expected version; concurrent step progression can make the
  requested target invalid.
idempotency: No idempotency key; repeating the same backward target is rejected once
  it is no longer earlier.
retry_behavior: Refresh run status and current_step_index before retrying.
reversibility: A later forward progression is not an inverse and does not restore
  external effects.
audit_evidence_ids:
- EVD-45
transition:
  from_state_id: STA-2
  to_state_id: STA-2
carrier_relationship:
  command_group: transition_back
  paired_effect_action_ids:
  - ACT-37
  affected_carrier: legacy run row; current_step_index
enforced_authentication: Optional global valid-bearer gate
enforced_authorization: absent
domain_eligibility: Legacy run with an earlier in-bounds target step.
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: unexercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-36 implementation status present: ACT-36 Preserve legacy run carrier during transition-back
    is present: SER rows SER-0052 inspect src/orchestrator/api/routers/runs.py::transition_back and establish
    the record proposition under EVD-45.'
  test: 'ACT-36 test status unexercised: ACT-36 Preserve legacy run carrier during transition-back remains
    unexercised: bounded SER rows SER-0053 cover tests/integration/test_api_backward_transitions.py::test_transition_backward_basic
    but do not assert the complete record proposition.'
  documentation: 'ACT-36 documentation status documented: ACT-36 Preserve legacy run carrier during transition-back
    is documented at reality/actions/act-36-transition-back-run-preserved.yaml and by EVD-45; any listed
    question (Q-5 (open: Define product-role authorization before treating actor text as authority)) is
    explicitly disclosed and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-36 capability status unknown: ACT-36 Preserve legacy run carrier during transition-back
    remains unknown as a user capability: no CAP implementation_carrier_binding; EVD-45 establishes the
    record carrier but no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-36 epistemic status observed: ACT-36 Preserve legacy run carrier during transition-back
    is observed from direct/audit authority EVD-45 (EVD-45 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#run-and-lifecycle-capability)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/runs.py::transition_back
test_locators: []
bounded_test_locators:
- tests/integration/test_api_backward_transitions.py::test_transition_backward_basic
