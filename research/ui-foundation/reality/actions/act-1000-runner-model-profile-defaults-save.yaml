{id: ACT-1000, implementation_status: present, capability_status: current, executable: true, command_id: CMD-929, actor: caller, permission_requirements: [PER-900, PER-902], preconditions: ["runner type is selectable", "body runner type equals path runner type"], expected_source_version: "None; no defaults-row version", required_input: {runner_type: required, model_profile_defaults: required}, validation: ["selectable runner type", "body/path runner-type equality", "model-profile mapping schema"], durable_effect: "Synchronously replaces persisted model-profile default rows for the selected runner and returns the normalized mapping; future resolution only.", resulting_state_id: STA-946, failure_modes: ["422 type mismatch or schema validation", "database error"], stale_state_behavior: "Replacement is not version-fenced; concurrent saves can overwrite mappings.", idempotency: "Identical replacement converges to the same mapping; no key.", retry_behavior: "Reload server defaults before retry.", reversibility: "A later save can replace defaults but no history/undo is established.", audit_evidence_ids: [EVI-927], transition: {from_state_id: STA-946, to_state_id: STA-946}, enforced_authentication: "optional JWT", enforced_authorization: absent, domain_eligibility: "PER-902 selectable-runner and request-schema prerequisites", proposed_role_policy: null}
