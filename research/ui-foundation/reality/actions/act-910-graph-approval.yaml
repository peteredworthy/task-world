{id: ACT-910, implementation_status: present, capability_status: current, executable: true, command_id: CMD-902, actor: "caller-supplied decider", permission_requirements: [PER-902], preconditions: ["eligible approval gate and graph state"], expected_source_version: "No client graph position", required_input: {decision_type: approval, value: "approved|rejected", target: required}, validation: ["strict RecordDecisionCommand and graph target/value checks"], durable_effect: "Records approval; approved may bind/release/complete, rejected required input can fail node.", resulting_state_id: STA-927, failure_modes: ["404 graph", "409 stale/rejected", "422 schema"], stale_state_behavior: "Controller optimistic concurrency; rejection returns reason without universal recovery object.", idempotency: "No REST key", retry_behavior: "Refresh decision view.", reversibility: "No decision reversal", audit_evidence_ids: [EVI-901, EVI-902], transition: {from_state_id: STA-927, to_state_id: STA-927}, enforced_authentication: "optional JWT", enforced_authorization: absent, domain_eligibility: "graph actor/target rules", proposed_role_policy: null}
