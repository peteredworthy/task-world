{id: ACT-983, implementation_status: present, capability_status: current, executable: true, command_id: CMD-920, actor: caller, permission_requirements: [PER-900, PER-902], preconditions: ["run worktree exists", "HEAD is a merge commit from a prior back-merge"], expected_source_version: "None; HEAD is read at execution", required_input: {run_id: required}, validation: ["worktree", "HEAD parent count"], durable_effect: "Synchronously creates a git revert commit for the qualifying worktree-head merge and emits BackMergeReverted.", resulting_state_id: STA-938, failure_modes: ["409 no qualifying HEAD merge", "git error"], stale_state_behavior: "A changed HEAD can make the selected merge ineligible; no expected revision is supplied.", idempotency: "Not idempotent after HEAD changes; no key.", retry_behavior: "Inspect HEAD and revert result before retry.", reversibility: "Compensates only the identified back-merge; it is not a general branch restoration.", audit_evidence_ids: [EVI-922], transition: {from_state_id: STA-938, to_state_id: STA-938}, enforced_authentication: "optional JWT", enforced_authorization: absent, domain_eligibility: "PER-902 qualifying HEAD merge prerequisite", proposed_role_policy: null}
