id: ACT-23
title: Approve legacy task awaiting approval
definition: Approves one legacy task in pending-user-action approval state and completes that task.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-29
actor: fixed or injected user
permission_requirements:
- PER-3
preconditions:
- legacy task is pending user action for approval
expected_source_version: No client task version
required_input:
  run_id: required
  task_id: required
  comment: optional
validation:
- legacy execution mode
- task approval transition validation
durable_effect: Records an ApprovalDecision and changes the task from pending user action to completed.
resulting_state_id: STA-14
failure_modes:
- 404 run or task identity
- 409 task is not awaiting approval
stale_state_behavior: An advanced task is rejected by the task transition check.
idempotency: No idempotency key; a completed task cannot be approved again.
retry_behavior: Refresh task state before retrying an uncertain response.
reversibility: No approval-reversal command.
audit_evidence_ids:
- EVD-12
transition:
  from_state_id: STA-11
  to_state_id: STA-14
enforced_authentication: Optional global valid-bearer gate
enforced_authorization: absent
domain_eligibility: Legacy task approval state and transition checks.
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: ACT-23 records approval and completes pending-user-action task.
  test: The test invokes approval and asserts persisted completed task.
  documentation: ACT-23 task approval is documented.
  capability: Approval attribution is not product-role authorization.
  epistemic: ACT-23 is observed by EVD-12.
implementation_locators:
- src/orchestrator/api/routers/tasks.py::approve_task
test_locators:
- tests/integration/test_approval_workflow.py::test_approve_task_transitions_to_completed
