id: ACT-968
implementation_status: present
capability_status: current
executable: true
command_id: CMD-905
actor: "REST caller; graph cancellation is a mode-specific consequence and does not grant a product role."
permission_requirements: [PER-900, PER-901, PER-902, PER-903]
preconditions: ["ACT-933 accepted REST cancellation from a paused legacy row.", "The run is graph mode and its graph run is cancelling."]
expected_source_version: "The REST cancel request has no client run or graph version; its acceptance is source-preserving on the legacy row while graph cancellation applies separately."
required_input: {run_id: required}
validation: ["CMD-905 legacy cancel state validation and signal enqueue", "PER-903 graph cancellation lifecycle validation"]
durable_effect: "The graph cancellation consequence transitions the graph-run carrier from cancelling to cancelled before the later legacy-row consumer application."
resulting_state_id: STA-953
failure_modes: ["404 missing run/graph", "409 stopping or illegal cancellation state", "executor cancellation failure"]
stale_state_behavior: "REST acceptance can be redelivered or encounter an already-applied lifecycle state; it does not cross from graph run to legacy row state."
idempotency: "No idempotency key; lifecycle/domain state makes a repeat after cancellation ineligible or a no-op at its consumer boundary."
retry_behavior: "Read graph run and legacy activity projections; do not infer graph completion from the REST 202 acceptance alone."
reversibility: "No direct uncancel; recovery is separate and does not undo graph cancellation."
audit_evidence_ids: [EVI-904, EVI-913, EVI-931, EVI-932, EVI-933]
transition: {from_state_id: STA-921, to_state_id: STA-953}
carrier_relationship: {kind: paired graph-run terminal consequence, paired_graph_run_entry_action_id: ACT-971, paired_node_consequence_action_id: ACT-969, rest_acceptance_action_id: ACT-933, rest_acceptance_transition: {from_state_id: STA-903, to_state_id: STA-903}, rest_eventual_application_transition: {from_state_id: STA-903, to_state_id: STA-906}, ordering: "ACT-971 first transitions the graph run paused-to-cancelling; this CMD-905 consequence then transitions cancelling-to-cancelled, while ACT-969 separately cancels leased/running/suspended nodes before later row application."}
enforced_authentication: "PER-900 optional global valid-bearer gate."
enforced_authorization: absent
domain_eligibility: "PER-902 validates paused legacy-row cancellation; PER-903 validates the graph cancellation consequence."
proposed_role_policy: null
