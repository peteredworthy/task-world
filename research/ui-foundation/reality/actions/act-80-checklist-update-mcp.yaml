id: ACT-80
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-50
actor: public MCP caller
permission_requirements: [PER-1, PER-5, PER-3]
preconditions: [tool is exposed by scope, active mutable task]
expected_source_version: 'None'
required_input: {run_id: required, task_id: required, req_id: required, status: required, note: optional}
validation: [scope-exposure, handler-and-workflow-validation]
durable_effect: Dispatches public MCP checklist update to the mutable workflow boundary.
resulting_state_id: STA-9
failure_modes: [tool unavailable, handler error, transition error]
stale_state_behavior: Handler applies current task eligibility.
idempotency: Repeating same update converges where accepted.
retry_behavior: Refresh task and invoke only if tool remains exposed.
reversibility: Later eligible update may supersede it.
audit_evidence_ids: [EVD-123, EVD-124, EVD-125]
transition: {from_state_id: STA-9, to_state_id: STA-9}
carrier_relationship:
  affected_carrier: checklist item status and note while the representative BUILDING task status is preserved
  caveat: This is a field-mutation carrier self-loop, not a task lifecycle transition.
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-5 scoped exposure plus PER-3 task eligibility
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-80 implementation status present: ACT-80 Dispatches public MCP checklist update
    to the mutable workflow boundary. is present: SER rows SER-0470, SER-0477 inspect src/orchestrator/api/mcp/server.py::OrchestratorMCPServer._register_tools.orchestrator_update_checklist,
    src/orchestrator/api/mcp/tools.py::ToolHandler._update_checklist and establish the record proposition
    under EVD-123, EVD-124.'
  test: 'ACT-80 test status exercised: ACT-80 Dispatches public MCP checklist update to the mutable workflow
    boundary. is exercised because admitted SER rows SER-0490 contain direct assertions at tests/integration/test_mcp_server.py::test_full_workflow_through_mcp_server;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-80 documentation status documented: ACT-80 Dispatches public MCP checklist update
    to the mutable workflow boundary. is documented at reality/actions/act-80-checklist-update-mcp.yaml
    and by EVD-123, EVD-124, EVD-125; any listed question (Q-5 (open: Define product-role authorization before treating
    actor text as authority)) is explicitly disclosed and does not create contradictory documentation
    for this bounded proposition.'
  capability: 'ACT-80 capability status unknown: ACT-80 Dispatches public MCP checklist update to the
    mutable workflow boundary. remains unknown as a user capability: no CAP implementation_carrier_binding;
    EVD-123, EVD-124, EVD-125 establish the record carrier but no relevant CAP authority classifies this record as current
    or derived.'
  epistemic: 'ACT-80 epistemic status observed: ACT-80 Dispatches public MCP checklist update to the mutable
    workflow boundary. is observed from direct implementation/test authority EVD-123, EVD-124, EVD-125,
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/mcp/tools.py::ToolHandler._update_checklist
- src/orchestrator/api/mcp/server.py::OrchestratorMCPServer._register_tools.orchestrator_update_checklist
test_locators:
- tests/integration/test_mcp_server.py::test_full_workflow_through_mcp_server
bounded_test_locators: []
