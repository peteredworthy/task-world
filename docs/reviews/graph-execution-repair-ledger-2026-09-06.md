# Graph execution repair requirements ledger — 6 September 2026

This ledger tracks repairs for run `ca8faa94-f22c-4645-aab6-b5d6aca56d4b` on
source commit `08c8cd5e6`. Regression tests are supporting evidence; a joined
production-path exercise is required before a row is validated. No live-model
reliability claim is in scope.

| ID | Required behavior and acceptance invariant | Implementation evidence | Product-path proof | Regression evidence | Status | Remaining gap |
|---|---|---|---|---|---|---|
| GATE-ENV | Submission-gate isolation preserves per-database journals, stays isolated from production journals and the leased checkout, and does not manufacture supervisor failures. | Gate subprocesses retain test-owned journal routing; disposable HOME/XDG/cache/temp and checkout isolation remain. Nested cleanup now compares parsed registered worktree paths. | The production gate executes the four journal cases, cleanup case, and three supervisor cases in a nested committed checkout. It also auto-resolved and ran task-world's exact checked-in command against commit `999bb360b`. | Direct journal `5 passed`; direct supervisor `3 passed`; nested actual-gate wrapper `1 passed` with all eight inner tests passing; exact configured gate `5930 passed, 5 skipped`. | Validated | None offline. |
| FEEDBACK | A rejected submission returns bounded structured category, command, status/exit or timeout, failed test IDs, useful tail diagnostics, explicit truncation, hashes, and durable audit reference through the Codex tool response. | Typed acknowledgement/evidence models carry bounded late diagnostics, failed IDs, truncation, byte/hash integrity, semantic identity, and `graph-event:<run>:<position>`. | A concrete scripted JSON-RPC Codex transport submits through the real dispatch callback and gate. | `test_codex_execute_retains_late_real_gate_failure_and_durable_audit`; callback file included in the 158-test focused pass. | Validated | None offline. |
| OUTCOME | Normal agent return after rejection preserves the first meaningful typed cause, latest diagnostics, rejected candidate, lease cleanup, and public readback; process loss and cancellation remain distinct. | Public `SubmissionRejectedError`; dominant typed outcome selection; durable recovery snapshot, first cause, latest evidence, and runtime-health fields. | Joined scenario returns normally after rejection and reads the typed cause/candidate with zero active leases; existing restart/cancel paths stay distinct. | `test_normal_return_after_gate_rejection_preserves_candidate_failure` plus joined sequential, cancellation, and restart coverage in the 158-test pass. | Validated | None offline. |
| BLOCKAGE | An unchanged validation-environment fault blocks without rebuilding; explicit environmental repair and snapshot resolution can continue; transient retries remain finite. | Semantic environment blockage is non-retryable and releases the lease. An identity- and position-checked operator command restores either exact baseline or rejected snapshot through a durable outbox effect. | Public API scenario repairs an external sentinel, selects the rejected snapshot, dispatches exact restoration, resumes, and accepts with two executions but one implementation mutation. | `test_api_environment_blockage_restores_rejected_candidate_and_continues` (`1 passed`) and boundary/replay tests. | Validated | Operator intervention is intentionally required. |
| BASELINE | Semantic failure identity excludes timings, paths, ordering, and passing-test noise; changed/added failures differ; incomplete/timeout evidence is not declared equivalent; baseline failure is not an automatic exemption. | Semantic fingerprint is separate from full-output integrity hashes and is available only for complete structured failures; authorization remains explicit. Missing feature tests remain candidate failures. | The joined scenario classifies the same stable project-test failure as environment blockage without waiving the gate. | Semantic identity, changed/added failure, missing test, timeout, and explicit exemption unit cases in the 158-test pass. | Validated | None offline. |
| VERIFIER | Verifier prompt renders semantic stage, objective, acceptance obligations, stage-specific rubric, exact bound artifact/candidate, and requirement evidence. Plan review assesses coverage/feasibility rather than finished implementation. | Complete verifier packet plus deterministic plan-review rubric; incomplete plan contracts fail before runner dispatch. | Exact incident fixture is rendered and a production dispatch test proves an incomplete contract executes no runner, creates no retry, and releases its lease. | Independent verifier pass: `49 passed`; included in focused suite. | Validated | None offline. |
| JOINED | Default-collected real-object test crosses production API, prompts, callback, real submission gate with a committed project test command, recovery, public readback, environmental repair, correction, and acceptance. | Scripted runners consume real prompts/tool responses; production API, driver, dispatcher, gate, projection, and outbox objects are used. | Joined scenario covers plan-before-implementation, typed gate rejection, normal return, retained snapshot, operator repair/restoration, continuation, verifier, and acceptance. Repository-specific focused regressions run through the nested gate test; the exact checked-in command was separately run through the production gate against the durable repair commit. | Joined test `1 passed in 39.16s`; adjacent correction tests `2 passed`; focused aggregate `158 passed, 101 deselected`; configured production gate `5930 passed, 5 skipped`. | Validated | A paid live-model run remains unproven and is not claimed. |
| DOCS | Diagnosis contains a resolution matrix mapping issues to implementation, exact tests, actual results, and remaining live qualification. | Resolution addendum and bounded JSON evidence added. | Results distinguish production defects, test defect, runner-environment observations, and live qualification. | Document inspection and JSON parsing after full-suite and configured-gate completion. | Validated | None. |

## Validation gate

- Focused regressions for every row.
- Relevant submission, recovery, sequential product-path, cancellation, and restart tests.
- `uv run pytest` with default collection.
- `uv run ruff check .`
- `uv run pyright`
- `uv run python scripts/check_graph_projection_boundaries.py`
