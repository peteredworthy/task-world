id: ACT-4
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-15
actor: caller
permission_requirements:
- PER-1
- PER-7
preconditions:
- eligible active/paused run
- worktree/source branch
expected_source_version: None
required_input:
  run_id: required
validation:
- run/branch readiness
durable_effect: Leaves source-to-run merge conflict in progress for resolution.
resulting_state_id: STA-43
failure_modes:
- 409/400
- git error
stale_state_behavior: No branch version.
idempotency: Git dependent.
retry_behavior: Resolve/abort conflict before retry.
reversibility: Conflict is unresolved, not merge applied.
audit_evidence_ids:
- EVD-29
transition:
  from_state_id: STA-59
  to_state_id: STA-43
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-7 git prerequisites
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: unexercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-4 implementation status present: ACT-4 Leaves source-to-run merge conflict in progress
    for resolution. is present: SER rows SER-0060 inspect src/orchestrator/api/routers/runs.py::back_merge_endpoint
    and establish the record proposition under EVD-29.'
  test: 'ACT-4 test status unexercised: ACT-4 Leaves source-to-run merge conflict in progress for resolution.
    remains unexercised because no admitted exact test locator asserts its complete record proposition
    under EVD-29.'
  documentation: 'ACT-4 documentation status documented: ACT-4 Leaves source-to-run merge conflict in
    progress for resolution. is documented at reality/actions/act-4-detail-pull-upstream-conflict.yaml
    and by EVD-29; any listed question (no canonical unresolved conflict or question) is explicitly disclosed
    and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-4 capability status unknown: ACT-4 Leaves source-to-run merge conflict in progress
    for resolution. remains unknown as a user capability: no CAP implementation_carrier_binding; EVD-29
    establishes the record carrier but no relevant CAP authority classifies this record as current or
    derived.'
  epistemic: 'ACT-4 epistemic status observed: ACT-4 Leaves source-to-run merge conflict in progress for
    resolution. is observed from direct/audit authority EVD-29 (EVD-29 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#repository-review-sourceconfiguration-and-file-effects)),
    with no canonical conflict/question qualification; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/runs.py::back_merge_endpoint
test_locators: []
bounded_test_locators: []
