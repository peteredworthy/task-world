id: ACT-6
title: Create run
definition: Synchronously create a legacy run from absent identity, return the created-response
  carrier, and persist a distinct draft row.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-1
actor: caller
permission_requirements:
- PER-1
- PER-2
- PER-3
preconditions:
- Exactly one routine source
- selectable runner and valid configuration
expected_source_version: No source row exists for the new run identity.
required_input:
  routine_source: required
  runner: required
validation:
- CreateRunRequest schema
- runner selection validation
durable_effect: Creates a run row and creation events; the synchronous response confirms
  creation, and the durable row is draft.
resulting_state_id: STA-57
failure_modes:
- 422 malformed request
- 404 routine
- runner unavailable
stale_state_behavior: No source version; duplicate creation creates distinct runs.
idempotency: No idempotency key.
retry_behavior: Retry after unknown response can create another run.
reversibility: Delete is separate and status-limited.
audit_evidence_ids:
- EVD-43
- EVD-38
- EVD-39
- EVD-40
transition:
  from_state_id: STA-56
  to_state_id: STA-57
created_draft_transition:
  from_state_id: STA-57
  to_state_id: STA-1
  evidence_ids:
  - EVD-43
enforced_authentication: Optional global valid-bearer gate; disabled by default.
enforced_authorization: absent
domain_eligibility: No pre-existing source row for this creation; exactly one routine
  source, selectable runner, valid configuration, and request schema.
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-6 implementation status present: ACT-6 Create run is present: SER rows SER-0099
    inspect src/orchestrator/api/routers/runs.py::create_run and establish the record proposition under
    EVD-43, EVD-38, EVD-39, EVD-40.'
  test: 'ACT-6 test status exercised: ACT-6 Create run is exercised because admitted SER rows SER-0100
    contain direct assertions at tests/integration/test_api_runs.py::test_create_run; no active rejected
    locator defeats that proof.'
  documentation: 'ACT-6 documentation status documented: ACT-6 Create run is documented at reality/actions/act-6-create-run.yaml
    and by EVD-43, EVD-38, EVD-39, EVD-40; any listed question (Q-5 (open: Define product-role authorization
    before treating actor text as authority)) is explicitly disclosed and does not create contradictory
    documentation for this bounded proposition.'
  capability: 'ACT-6 capability status unknown: ACT-6 Create run remains unknown as a user capability:
    no CAP implementation_carrier_binding; EVD-43, EVD-38, EVD-39, EVD-40 establishes the record carrier
    but no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-6 epistemic status observed: ACT-6 Create run is observed from direct/audit authority
    EVD-43, EVD-38, EVD-39, EVD-40 (EVD-43 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#run-and-lifecycle-capability);
    EVD-38 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#authority-model);
    EVD-39 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#authority-model);
    EVD-40 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#legacy-versus-graph-mode)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/runs.py::create_run
test_locators:
- tests/integration/test_api_runs.py::test_create_run
bounded_test_locators: []
