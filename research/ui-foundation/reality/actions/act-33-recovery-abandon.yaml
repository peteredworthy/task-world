id: ACT-33
title: Abandon legacy recovering task
definition: Completes recovery with abandon and fails the recovering legacy task.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-9
actor: caller
permission_requirements:
- PER-3
preconditions:
- legacy task is recovering
expected_source_version: No client task version
required_input:
  run_id: required
  task_id: required
  notes: optional
validation:
- complete-recovery outcome abandon
- recovering-task state validation
durable_effect: Records failed outcome and notes and changes the task to failed.
resulting_state_id: STA-15
failure_modes:
- 404 run or task identity
- 409 task is not recovering
- 400 invalid REST outcome
stale_state_behavior: A task advanced beyond recovering is rejected.
idempotency: No idempotency key; a failed task is no longer a legal recovery source.
retry_behavior: Refresh task state before resending.
reversibility: No direct inverse for an abandoned recovery outcome.
audit_evidence_ids:
- EVD-14
transition:
  from_state_id: STA-12
  to_state_id: STA-15
enforced_authentication: Optional global valid-bearer gate
enforced_authorization: absent
domain_eligibility: Legacy recovering task only.
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-33 implementation status present: ACT-33 Abandon legacy recovering task is present:
    SER rows SER-0046 inspect src/orchestrator/workflow/service.py::WorkflowService.complete_recovery_abandon
    and establish the record proposition under EVD-14.'
  test: 'ACT-33 test status exercised: ACT-33 Abandon legacy recovering task is exercised because admitted
    SER rows SER-0047 contain direct assertions at tests/integration/test_workflow_service.py::test_complete_recovery_abandon_projects_failed_attempt_without_save_run;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-33 documentation status documented: ACT-33 Abandon legacy recovering task is documented
    at reality/actions/act-33-recovery-abandon.yaml and by EVD-14; any listed question (Q-5 (open: Define
    product-role authorization before treating actor text as authority)) is explicitly disclosed and does
    not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-33 capability status unknown: ACT-33 Abandon legacy recovering task remains unknown
    as a user capability: CAP-39=unknown as action-contract; CAP-44=gap as reversibility-action; CAP-61=unknown
    as action-contract; EVD-14 establishes the record carrier but no relevant CAP authority classifies
    this record as current or derived.'
  epistemic: 'ACT-33 epistemic status observed: ACT-33 Abandon legacy recovering task is observed from
    direct/audit authority EVD-14 (EVD-14 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#legacy-task-approval-clarification-and-retry-capability)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/workflow/service.py::WorkflowService.complete_recovery_abandon
test_locators:
- tests/integration/test_workflow_service.py::test_complete_recovery_abandon_projects_failed_attempt_without_save_run
bounded_test_locators: []
