id: ACT-85
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-55
actor: local CLI operator
permission_requirements: [PER-7]
preconditions: [backup metadata path exists, writable target database]
expected_source_version: 'None'
required_input: {backup_meta_path: required, target_db: optional}
validation: [click-existing-path, backup-metadata]
durable_effect: Replaces the selected local database from backup metadata.
resulting_state_id: STA-74
failure_modes: [BackupError, filesystem error]
stale_state_behavior: Restore has no version fence and may overwrite a changed target.
idempotency: Repeating same backup restore converges to backup contents.
retry_behavior: Correct metadata or target failure and retry locally.
reversibility: Restore a different backup; prior target is not retained by this command.
audit_evidence_ids: [EVD-114]
transition: {from_state_id: STA-74, to_state_id: STA-74}
carrier_relationship:
  affected_carrier: selected target database file contents
  caveat: This file-content result-carrier self-loop is non-lifecycle and does not reuse the managed-environment restore state.
enforced_authentication: none local CLI
enforced_authorization: absent
domain_eligibility: PER-7 local backup path prerequisites; local CLI has no product authorization.
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: unexercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-85 implementation status present: ACT-85 Replaces the selected local database from
    backup metadata. is present: SER rows SER-0475 inspect src/orchestrator/cli/db.py::restore_backup_cmd
    and establish the record proposition under EVD-114.'
  test: 'ACT-85 test status unexercised: ACT-85 Replaces the selected local database from backup metadata.
    remains unexercised: bounded SER rows SER-0495 cover tests/unit/test_backup.py::test_restore_backup_copies_db_back
    but do not assert the complete record proposition.'
  documentation: 'ACT-85 documentation status documented: ACT-85 Replaces the selected local database
    from backup metadata. is documented at reality/actions/act-85-db-restore-backup-cli.yaml and by EVD-114;
    any listed question (Q-5 (open: Define product-role authorization before treating actor text as authority))
    is explicitly disclosed and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-85 capability status unknown: ACT-85 Replaces the selected local database from backup
    metadata. remains unknown as a user capability: no CAP implementation_carrier_binding; EVD-114 establishes
    the record carrier but no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-85 epistemic status observed: ACT-85 Replaces the selected local database from backup
    metadata. is observed from direct/audit authority EVD-114 (EVD-114 (implementation: src/orchestrator/cli/db.py)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators: [src/orchestrator/cli/db.py::restore_backup_cmd]
test_locators: []
bounded_test_locators:
- tests/unit/test_backup.py::test_restore_backup_copies_db_back
