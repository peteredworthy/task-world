{id: ACT-918, implementation_status: present, capability_status: current, executable: true, command_id: CMD-903, actor: caller, permission_requirements: [PER-900], preconditions: ["operation-specific identity and schema checks"], expected_source_version: "None", required_input: {operation: "archive|unarchive|agent_create|agent_update|agent_delete|agent_reset|runner_defaults_put|repo_add|repo_remove", payload: required}, validation: ["routine, agent name/profile, runner default, repository URL/path/name schemas"], durable_effect: "Changes routine metadata, agent/default rows, or repository filesystem/metadata.", resulting_state_id: STA-927, failure_modes: ["409 duplicate agent", "404 identity", "validation/path/filesystem errors"], stale_state_behavior: "No action version/idempotency; UI often discards normalized result and some failures.", idempotency: "Operation-specific without shared key", retry_behavior: "Refresh target list/record before retry.", reversibility: "Archive is compensable by unarchive; deletes/removal lack undo.", audit_evidence_ids: [EVI-903], transition: {from_state_id: STA-927, to_state_id: STA-927}, enforced_authentication: "optional JWT", enforced_authorization: absent, domain_eligibility: "ordinary route/domain checks", proposed_role_policy: null}
