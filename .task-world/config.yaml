# Pre-run health check: only run unit + integration tests (e2e tests are flaky
# due to server startup timeouts and should not gate agent runs).
test_command: "uv run pytest tests/unit tests/integration --tb=no -q --ignore=tests/integration/test_full_persistence.py"
