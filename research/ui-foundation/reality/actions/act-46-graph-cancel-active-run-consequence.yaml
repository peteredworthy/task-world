id: ACT-46
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-6
actor: REST caller; graph cancellation is a mode-specific consequence and does not
  grant a product role.
permission_requirements:
- PER-1
- PER-2
- PER-3
- PER-4
preconditions:
- ACT-20 accepted REST cancellation from an active legacy row.
- The run is graph mode and its graph run is cancelling.
expected_source_version: The REST cancel request has no client run or graph version;
  its acceptance is source-preserving on the legacy row while graph cancellation applies
  separately.
required_input:
  run_id: required
validation:
- CMD-6 legacy cancel state validation and signal enqueue
- PER-4 graph cancellation lifecycle validation
durable_effect: The graph cancellation consequence transitions the graph-run carrier
  from cancelling to cancelled before the later legacy-row consumer application.
resulting_state_id: STA-25
failure_modes:
- 404 missing run/graph
- 409 stopping or illegal cancellation state
- executor cancellation failure
stale_state_behavior: REST acceptance can be redelivered or encounter an already-applied
  lifecycle state; it does not cross from graph run to legacy row state.
idempotency: No idempotency key; lifecycle/domain state makes a repeat after cancellation
  ineligible or a no-op at its consumer boundary.
retry_behavior: Read graph run and legacy activity projections; do not infer graph
  completion from the REST 202 acceptance alone.
reversibility: No direct uncancel; recovery is separate and does not undo graph cancellation.
audit_evidence_ids:
- EVD-11
- EVD-20
- EVD-38
- EVD-39
- EVD-40
transition:
  from_state_id: STA-22
  to_state_id: STA-25
carrier_relationship:
  kind: paired graph-run terminal consequence
  paired_graph_run_entry_action_id: ACT-50
  paired_node_consequence_action_id: ACT-47
  rest_acceptance_action_id: ACT-20
  rest_acceptance_transition:
    from_state_id: STA-2
    to_state_id: STA-2
  rest_eventual_application_transition:
    from_state_id: STA-2
    to_state_id: STA-7
  ordering: ACT-50 first transitions the graph run active-to-cancelling; this CMD-6
    consequence then transitions cancelling-to-cancelled, while ACT-47 separately
    cancels leased/running/suspended nodes before later row application.
enforced_authentication: PER-1 optional global valid-bearer gate.
enforced_authorization: absent
domain_eligibility: PER-3 validates active legacy-row cancellation; PER-4 validates
  the graph cancellation consequence.
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-46 implementation status present: ACT-46 The graph cancellation consequence transitions
    the graph-run carrier from cancelling to cancelled before the later legacy-row consumer application.
    is present: SER rows SER-0073 inspect src/orchestrator/graph/_commands.py::RUN_LIFECYCLE_TRANSITIONS
    and establish the record proposition under EVD-11, EVD-20, EVD-38, EVD-39, EVD-40.'
  test: 'ACT-46 test status exercised: ACT-46 The graph cancellation consequence transitions the graph-run
    carrier from cancelling to cancelled before the later legacy-row consumer application. is exercised
    because admitted SER rows SER-0074 contain direct assertions at tests/unit/test_graph_commands.py::test_lifecycle_legal_transitions;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-46 documentation status documented: ACT-46 The graph cancellation consequence transitions
    the graph-run carrier from cancelling to cancelled before the later legacy-row consumer application.
    is documented at reality/actions/act-46-graph-cancel-active-run-consequence.yaml and by EVD-11, EVD-20,
    EVD-38, EVD-39, EVD-40; any listed question (no canonical unresolved conflict or question) is explicitly
    disclosed and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-46 capability status unknown: ACT-46 The graph cancellation consequence transitions
    the graph-run carrier from cancelling to cancelled before the later legacy-row consumer application.
    remains unknown as a user capability: no CAP implementation_carrier_binding; EVD-11, EVD-20, EVD-38,
    EVD-39, EVD-40 establishes the record carrier but no relevant CAP authority classifies this record
    as current or derived.'
  epistemic: 'ACT-46 epistemic status observed: ACT-46 The graph cancellation consequence transitions
    the graph-run carrier from cancelling to cancelled before the later legacy-row consumer application.
    is observed from direct/audit authority EVD-11, EVD-20, EVD-38, EVD-39, EVD-40 (EVD-11 (audit-report:
    research/ui-foundation/agent-reports/03-workflow-state.md#legacy-versus-graph-mode); EVD-20 (audit-report:
    research/ui-foundation/agent-reports/02-graph-runtime.md#command_specs-exhaustive-command-to-effect-table);
    EVD-38 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#authority-model);
    EVD-39 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#authority-model);
    EVD-40 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#legacy-versus-graph-mode)),
    with no canonical conflict/question qualification; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/graph/_commands.py::RUN_LIFECYCLE_TRANSITIONS
test_locators:
- tests/unit/test_graph_commands.py::test_lifecycle_legal_transitions
bounded_test_locators: []
