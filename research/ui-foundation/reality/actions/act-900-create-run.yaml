id: ACT-900
title: Create run
definition: Synchronously create a legacy run from absent identity, return the created-response carrier, and persist a distinct draft row.
implementation_status: present
capability_status: current
executable: true
command_id: CMD-900
actor: caller
permission_requirements: [PER-900, PER-901, PER-902]
preconditions: ["Exactly one routine source", "selectable runner and valid configuration"]
expected_source_version: "No source row exists for the new run identity."
required_input: {routine_source: required, runner: required}
validation: ["CreateRunRequest schema", "runner selection validation"]
durable_effect: "Creates a run row and creation events; the synchronous response confirms creation, and the durable row is draft."
resulting_state_id: STA-955
failure_modes: ["422 malformed request", "404 routine", "runner unavailable"]
stale_state_behavior: "No source version; duplicate creation creates distinct runs."
idempotency: "No idempotency key."
retry_behavior: "Retry after unknown response can create another run."
reversibility: "Delete is separate and status-limited."
audit_evidence_ids: [EVI-936, EVI-931, EVI-932, EVI-933]
transition: {from_state_id: STA-954, to_state_id: STA-955}
created_draft_transition: {from_state_id: STA-955, to_state_id: STA-900, evidence_ids: [EVI-936]}
enforced_authentication: "Optional global valid-bearer gate; disabled by default."
enforced_authorization: absent
domain_eligibility: "No pre-existing source row for this creation; exactly one routine source, selectable runner, valid configuration, and request schema."
proposed_role_policy: null
