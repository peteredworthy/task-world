id: ACT-49
implementation_status: present
capability_status: current
executable: true
command_id: CMD-6
actor: REST caller; this is the affected graph-node consequence of CMD-6, not an independent node command.
permission_requirements:
- PER-1
- PER-2
- PER-3
- PER-4
preconditions:
- ACT-51 has transitioned the graph run to cancelling and ACT-48 is applying its terminal graph-run consequence for ACT-21's
  accepted REST cancel.
- The affected graph node is leased, running, or suspended with an active/suspended lease.
expected_source_version: No client graph version is accepted; node cancellation is emitted during the same graph cancellation
  application.
required_input:
  run_id: required
  affected_node_id: required
validation:
- CMD-6 cancellation accepts the routed legacy lifecycle state
- PER-4 graph lease/node cancellation validation
durable_effect: Graph cancellation revokes the affected lease and transitions the affected graph-node carrier to cancelled
  without changing a legacy task or row carrier.
resulting_state_id: STA-35
failure_modes:
- 404 missing run/graph/node
- 409 illegal or already-applied cancellation state
- executor cancellation failure
stale_state_behavior: A later or duplicate cancellation observes the current graph node/lease state; it does not invent a
  cross-carrier recovery transition.
idempotency: No key; node and lease state make repeated cancellation application domain-dependent.
retry_behavior: Refresh graph node, lease, and graph-run projections; do not replay this consequence independently of cancellation.
reversibility: No direct node uncancel exists; any recovery path is separate graph work.
audit_evidence_ids:
- EVD-20
- EVD-38
- EVD-39
- EVD-40
transition:
  from_state_ids:
  - STA-30
  - STA-31
  - STA-32
  to_state_id: STA-35
carrier_relationship:
  kind: paired graph-node cancellation consequence
  paired_graph_run_entry_action_id: ACT-51
  paired_graph_run_terminal_action_id: ACT-48
  rest_acceptance_action_id: ACT-21
  command_id: CMD-6
  excluded_source_state_ids:
  - STA-28
  ordering: Only leased, running, or suspended nodes are directly cancelled by CMD-6 evidence. The graph node transitions
    on its own carrier while ACT-51/ACT-48 transition the graph run; neither action transitions to the legacy row.
enforced_authentication: PER-1 optional global valid-bearer gate.
enforced_authorization: absent
domain_eligibility: PER-3 REST cancellation eligibility and PER-4 graph node/lease cancellation rules.
proposed_role_policy: null
conflict_ids: []
