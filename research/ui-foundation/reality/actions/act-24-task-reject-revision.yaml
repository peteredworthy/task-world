id: ACT-24
title: Reject legacy task with revision attempts remaining
definition: Rejects one approval-pending legacy task when another revision attempt is available.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-30
actor: fixed or injected user
permission_requirements:
- PER-3
preconditions:
- legacy task is pending user action for approval
- current attempt is below the attempt limit
expected_source_version: No client task version
required_input:
  run_id: required
  task_id: required
  reason: optional
validation:
- legacy execution mode
- task rejection transition validation
- attempt limit evaluation
durable_effect: Records a rejected ApprovalDecision, creates the revision attempt, and returns the task
  to building.
resulting_state_id: STA-9
failure_modes:
- 404 run or task identity
- 409 task is not awaiting approval
stale_state_behavior: An advanced task is rejected by the task transition check.
idempotency: No idempotency key; a successful rejection changes state and creates a revision attempt.
retry_behavior: Refresh task state; retry is valid only while the task remains approval-pending.
reversibility: No direct reversal of the recorded rejection.
audit_evidence_ids:
- EVD-12
transition:
  from_state_id: STA-11
  to_state_id: STA-9
enforced_authentication: Optional global valid-bearer gate
enforced_authorization: absent
domain_eligibility: Legacy task approval state with an attempt remaining.
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: ACT-24 records rejection, creates revision attempt, and returns task to building.
  test: The test rejects below limit and asserts building plus new attempt.
  documentation: ACT-24 rejection/revision is documented.
  capability: One rejection path is not a complete decision-safety capability.
  epistemic: ACT-24 is observed by EVD-12.
implementation_locators:
- research/ui-foundation/reality/actions/act-24-task-reject-revision.yaml::durable_effect
- src/orchestrator/api/routers/tasks.py::reject_task
test_locators:
- tests/integration/test_approval_workflow.py::test_reject_task_transitions_to_building
