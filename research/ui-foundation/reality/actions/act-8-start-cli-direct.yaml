id: ACT-8
title: Start run through CLI direct path
definition: Apply legacy start synchronously through the divergent CLI path.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-3
actor: local CLI operator
permission_requirements:
- PER-3
preconditions:
- legacy run is draft
expected_source_version: None
required_input:
  run_id: required
validation:
- workflow transition validation
durable_effect: Directly invokes apply_start_run and persists lifecycle state.
resulting_state_id: STA-2
failure_modes:
- illegal transition
- local database failure
stale_state_behavior: Not REST signal acceptance; timing is intentionally distinct.
idempotency: State-validated; no request key.
retry_behavior: Read resulting row before retry.
reversibility: Pause or cancel are separate actions.
audit_evidence_ids:
- EVD-7
- EVD-40
transition:
  from_state_id: STA-1
  to_state_id: STA-2
enforced_authentication: No REST bearer gate; local CLI process directly applies the
  workflow transition.
enforced_authorization: absent
domain_eligibility: Legacy row must be draft.
proposed_role_policy: null
conflict_ids:
- CON-6
question_ids:
- Q-5
test_status: unexercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-8 implementation status present: ACT-8 Start run through CLI direct path is present:
    SER rows SER-0128 inspect src/orchestrator/cli/runs.py::start_run and establish the record proposition
    under EVD-7, EVD-40.'
  test: 'ACT-8 test status unexercised: ACT-8 Start run through CLI direct path remains unexercised because
    no admitted exact test locator asserts its complete record proposition under EVD-7, EVD-40.'
  documentation: 'ACT-8 documentation status documented: ACT-8 Start run through CLI direct path is documented
    at reality/actions/act-8-start-cli-direct.yaml and by EVD-7, EVD-40. CON-6 qualifies the separate REST
    timing contract and Q-5 leaves product-role authorization unresolved; neither negates this bounded synchronous
    CLI-start proposition.'
  capability: 'ACT-8 capability status unknown: ACT-8 Start run through CLI direct path remains unknown
    as a user capability: no CAP implementation_carrier_binding; EVD-7, EVD-40 establishes the record
    carrier but no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-8 epistemic status observed: ACT-8 Start run through CLI direct path is observed from
    direct/audit authority EVD-7, EVD-40 (EVD-7 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#state-carriers-and-legal-transitions);
    EVD-40 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#legacy-versus-graph-mode)),
    qualified by conflicts/questions [''CON-6'', ''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/cli/runs.py::start_run
test_locators: []
bounded_test_locators: []
