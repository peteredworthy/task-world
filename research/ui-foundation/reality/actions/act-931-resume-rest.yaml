id: ACT-931
title: Resume run through REST
definition: Accept a REST resume signal while preserving the paused legacy row until later consumer application.
implementation_status: present
capability_status: current
executable: true
command_id: CMD-904
actor: caller
permission_requirements: [PER-900, PER-901, PER-902]
preconditions: ["legacy run is paused", "resume runner and strategy are valid when supplied"]
expected_source_version: "No client run version."
required_input: {run_id: required, runner: optional, runner_config: optional, strategy: "continue|reset_worktree; optional"}
validation: ["legacy lifecycle state validation", "resume runner/config validation", "resume strategy validation"]
durable_effect: "Appends resume signal acceptance evidence and queues RESUME while the row remains paused; the consumer later applies paused to active."
resulting_state_id: STA-903
failure_modes: ["404 missing run", "409 stopping or illegal transition", "422 invalid resume request", "503 runner unavailable"]
stale_state_behavior: "Accepted RESUME preserves paused until consumer application; a stale or already-applied redelivery is acknowledged or discarded without a typed result receipt."
idempotency: "State-validated; no idempotency key. A repeated request after application is no longer eligible."
retry_behavior: "Poll row status and activity before retrying; reset_worktree can discard file state and is not retry-safe restoration."
reversibility: "Pause is a separate later action; reset_worktree effects are not undone by resume."
audit_evidence_ids: [EVI-900, EVI-912, EVI-931, EVI-932, EVI-933]
transition: {from_state_id: STA-903, to_state_id: STA-903}
eventual_applied_transition: {from_state_id: STA-903, to_state_id: STA-901, evidence_ids: [EVI-900]}
enforced_authentication: "Optional global valid-bearer gate; disabled by default."
enforced_authorization: absent
domain_eligibility: "Legacy row must be paused; graph failed reopening remains separately mode-qualified."
proposed_role_policy: null
