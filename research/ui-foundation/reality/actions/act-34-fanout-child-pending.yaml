id: ACT-34
title: Reset failed fan-out child to pending
definition: Fan-out retry resets the addressed failed child task to pending.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-10
actor: caller
permission_requirements:
- PER-3
preconditions:
- addressed legacy task is a failed fan-out child
expected_source_version: No client task or run version
required_input:
  run_id: required
  task_id: required
validation:
- legacy execution mode
- fan-out child identity validation
- failed child status validation
durable_effect: Resets the addressed child task from failed to pending and records task-status evidence.
resulting_state_id: STA-8
failure_modes:
- 404 run or child identity
- 409 task is not a failed fan-out child
stale_state_behavior: Targeted update rejects an ineligible child rather than overwriting concurrent child work.
idempotency: No idempotency key; a pending child is no longer a legal retry source.
retry_behavior: Refresh the child and parent fan-out state before retrying.
reversibility: No direct undo restores the failed child attempt.
audit_evidence_ids:
- EVD-44
transition:
  from_state_id: STA-15
  to_state_id: STA-8
carrier_relationship:
  command_group: fanout_retry
  paired_effect_action_ids:
  - ACT-35
  affected_carrier: fan-out child task
enforced_authentication: Optional global valid-bearer gate
enforced_authorization: absent
domain_eligibility: Failed legacy fan-out child only.
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: unexercised
