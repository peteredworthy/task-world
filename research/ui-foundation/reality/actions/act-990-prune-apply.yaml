{id: ACT-990, implementation_status: present, capability_status: current, executable: true, command_id: CMD-924, actor: caller, permission_requirements: [PER-900, PER-906], preconditions: ["preview/current git operation ready", "nonempty valid selection"], expected_source_version: "None", required_input: {run_id: required, files: required}, validation: ["selection", "base SHA"], durable_effect: "Synchronously creates prune commit effects and emits PruneApplied; returned UUID is not durable event identity.", resulting_state_id: STA-938, failure_modes: ["400 empty selection", "git error"], stale_state_behavior: "No version; preview can be stale.", idempotency: "Retry can add commits.", retry_behavior: "Inspect commits/worktree.", reversibility: "No prune undo endpoint.", audit_evidence_ids: [EVI-921], transition: {from_state_id: STA-961, to_state_id: STA-938}, enforced_authentication: "optional JWT", enforced_authorization: absent, domain_eligibility: "PER-906 review prerequisites", proposed_role_policy: null}
