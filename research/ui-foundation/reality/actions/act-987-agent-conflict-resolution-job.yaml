{id: ACT-987, implementation_status: present, capability_status: current, executable: true, command_id: CMD-934, actor: caller, permission_requirements: [PER-900, PER-906], preconditions: ["worktree exists", "unresolved conflicts exist", "runner dispatch available"], expected_source_version: "None", required_input: {run_id: required, agent_runner_type: optional, agent_runner_config: optional}, validation: ["conflicts", "runner selection", "dispatch"], durable_effect: "Synchronously accepts background conflict-agent dispatch, emits AgentFixStarted, and returns dispatched job_id.", resulting_state_id: STA-935, failure_modes: ["409 no conflicts/dispatch unavailable", "invalid runner", "later agent failure"], stale_state_behavior: "Dispatch is not a resolved conflict.", idempotency: "No key; repeat can dispatch another job.", retry_behavior: "Refresh conflicts/runner.", reversibility: "No dispatch undo.", audit_evidence_ids: [EVI-941], transition: {from_state_id: STA-934, to_state_id: STA-935}, enforced_authentication: "optional JWT", enforced_authorization: absent, domain_eligibility: "PER-906 review prerequisites", proposed_role_policy: null}
