{id: ACT-996, implementation_status: present, capability_status: current, executable: true, command_id: CMD-928, actor: caller, permission_requirements: [PER-900, PER-902], preconditions: ["agent name is unused"], expected_source_version: "None; create has no source version", required_input: {name: required, system_prompt: required, model_profile: required}, validation: ["create-agent request schema", "unique agent name"], durable_effect: "Synchronously creates and returns an agent configuration row.", resulting_state_id: STA-945, failure_modes: ["409 duplicate name", "422 schema validation", "database error"], stale_state_behavior: "Concurrent creates race on name uniqueness; no client version.", idempotency: "No key; duplicate name is rejected.", retry_behavior: "Refresh agent list/name availability before retry.", reversibility: "Separate delete can remove the row but cannot restore an identical deleted identity.", audit_evidence_ids: [EVI-926], transition: {from_state_id: STA-945, to_state_id: STA-945}, enforced_authentication: "optional JWT", enforced_authorization: absent, domain_eligibility: "PER-902 agent schema/name prerequisites", proposed_role_policy: null}
