id: ACT-962
implementation_status: present
capability_status: current
executable: true
command_id: CMD-912
actor: "Caller-supplied decider recorded by ACT-961; no authenticated-subject authorization is enforced."
permission_requirements: [PER-900, PER-901, PER-903]
preconditions: ["ACT-961's rejected required approval has been recorded.", "The required approval target remains eligible for the command's failure effect."]
expected_source_version: "This is the node-projection consequence of the same current-position decision command; no client graph position exists."
required_input: {decision_type: approval, value: rejected, target_node_id: required}
validation: ["The preceding RecordDecisionCommand approval validation succeeds.", "PER-903 applies the required-input target and state eligibility checks."]
durable_effect: "A rejected required approval dead-ends required input, releases its scoped lease where present, and projects the target graph node failed."
resulting_state_id: STA-930
failure_modes: ["409 when the decision command is rejected or loses its append race", "422 when the underlying approval input is invalid"]
stale_state_behavior: "A decision-command race prevents the node failure consequence; no legacy task or row state is changed."
idempotency: "No separate idempotency key exists for this data-dependent command consequence."
retry_behavior: "Read decision/node projections and follow the graph's separately validated recovery or topology path; do not replay a presumed failed decision."
reversibility: "No approval reversal exists; recovery or repair is a separate graph command."
audit_evidence_ids: [EVI-919, EVI-920]
transition: {from_state_id: STA-926, to_state_id: STA-930}
carrier_relationship: {kind: same-command node consequence, paired_decision_action_id: ACT-961, decision_transition: {from_state_id: STA-932, to_state_id: STA-933}, command_id: CMD-912, ordering: "The node was pending before the command; rejected decision recording and node failure are parallel carrier-local effects of one discriminated command."}
enforced_authentication: "PER-900 optional global valid-bearer gate."
enforced_authorization: absent
domain_eligibility: "PER-903 rejected required-approval target and graph-state eligibility."
proposed_role_policy: null
