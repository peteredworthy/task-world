id: ACT-27
title: Record legacy step approval
definition: Records approval for the current legacy step's human-approval gate.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-32
actor: caller-supplied approver
permission_requirements:
- PER-3
preconditions:
- target is the current actionable step
- step has a human-approval gate
expected_source_version: No client step or run version
required_input:
  run_id: required
  step_id: required
  approved_by: required
  comment: optional
validation:
- current actionable-step validation
- human-approval-gate validation
durable_effect: Records StepHumanApprovalRecorded on the step-approval carrier; an
  eligible paused run may separately resume.
resulting_state_id: STA-53
failure_modes:
- 404 run or step identity
- 409 non-current step or absent human-approval gate
stale_state_behavior: A non-current or no-longer-gated step is rejected.
idempotency: Repeated approval is last-write-wins on the step-approval carrier.
retry_behavior: Refresh the pending-action view before retrying.
reversibility: No legacy step-deny, defer, or approval-reversal command.
audit_evidence_ids:
- EVD-36
transition:
  from_state_id: STA-53
  to_state_id: STA-53
enforced_authentication: Optional global valid-bearer gate
enforced_authorization: absent
domain_eligibility: Current legacy step with a human-approval gate.
proposed_role_policy: null
conflict_ids: []
question_ids:
- Q-5
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-27 implementation status present: ACT-27 Record legacy step approval is present:
    SER rows SER-0033 inspect src/orchestrator/api/routers/runs.py::approve_step and establish the record
    proposition under EVD-36.'
  test: 'ACT-27 test status exercised: ACT-27 Record legacy step approval is exercised because admitted
    SER rows SER-0034 contain direct assertions at tests/integration/test_api_human_approval.py::test_approve_step_endpoint;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-27 documentation status documented: ACT-27 Record legacy step approval is documented
    at reality/actions/act-27-step-approve.yaml and by EVD-36. Q-5 leaves product-role authorization unresolved;
    an open question is a qualification and does not negate the bounded approval-recording proposition.'
  capability: 'ACT-27 capability status unknown: ACT-27 Record legacy step approval remains unknown as
    a user capability: no CAP implementation_carrier_binding; EVD-36 establishes the record carrier but
    no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-27 epistemic status observed: ACT-27 Record legacy step approval is observed from direct/audit
    authority EVD-36 (EVD-36 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#legacy-task-approval-clarification-and-retry-capability)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/runs.py::approve_step
test_locators:
- tests/integration/test_api_human_approval.py::test_approve_step_endpoint
bounded_test_locators: []
