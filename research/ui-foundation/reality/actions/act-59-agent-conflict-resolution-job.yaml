id: ACT-59
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-28
actor: caller
permission_requirements:
- PER-1
- PER-7
preconditions:
- worktree exists
- unresolved conflicts exist
- runner dispatch available
expected_source_version: None
required_input:
  run_id: required
  agent_runner_type: optional
  agent_runner_config: optional
validation:
- conflicts
- runner selection
- dispatch
durable_effect: Synchronously accepts background conflict-agent dispatch, emits AgentFixStarted, and returns
  dispatched job_id.
resulting_state_id: STA-39
failure_modes:
- 409 no conflicts/dispatch unavailable
- invalid runner
- later agent failure
stale_state_behavior: Dispatch is not a resolved conflict.
idempotency: No key; repeat can dispatch another job.
retry_behavior: Refresh conflicts/runner.
reversibility: No dispatch undo.
audit_evidence_ids:
- EVD-48
transition:
  from_state_id: STA-38
  to_state_id: STA-39
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-7 review prerequisites
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: unexercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: ACT-59 dispatches a background conflict-resolution agent and returns job_id.
  test: No exact route test asserts AgentFixStarted plus dispatched job_id.
  documentation: ACT-59 dispatch contract is documented.
  capability: Source-inspected dispatch is not proven available.
  epistemic: ACT-59 is observed by EVD-48.
implementation_locators:
- research/ui-foundation/reality/actions/act-59-agent-conflict-resolution-job.yaml::durable_effect
- research/ui-foundation/catalog/evidence.yaml::EVD-48
test_locators: []
