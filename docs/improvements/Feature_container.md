Instead of just focusing on adding "runs" we could have a wrapping concept of a feature. Within that we might have a chat where the feature itself was initially refined followed by an idea-to-plan run and then an actuall implementation run. It could even sporn more than one idea-to-plan and implementation run. The feature would keep the routine internally unless we choose to "publish" the routine making is visible for directly running in the repo, or even publish it back to the orchestrator to have it usable by others in other projects.
