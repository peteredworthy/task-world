id: ACT-56
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-16
actor: caller
permission_requirements:
- PER-1
- PER-7
preconditions:
- completed run
- worktree/source branch
- readiness
expected_source_version: None
required_input:
  run_id: required
  strategy: optional
  dirty_action: optional
validation:
- completed/readiness/request
durable_effect: Synchronously review-merges run branch into source branch.
resulting_state_id: STA-42
failure_modes:
- '409'
- '400'
- git error
stale_state_behavior: Preflight is not version-fenced.
idempotency: Git dependent.
retry_behavior: Refresh readiness/branches.
reversibility: No undo endpoint.
audit_evidence_ids:
- EVD-29
transition:
  from_state_id: STA-59
  to_state_id: STA-42
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-7 git/readiness prerequisites
proposed_role_policy: null
conflict_ids: []
question_ids: []
test_status: unexercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-56 implementation status present: ACT-56 Synchronously review-merges run branch
    into source branch. is present: SER rows SER-0094 inspect src/orchestrator/api/routers/runs.py::merge_back_endpoint
    and establish the record proposition under EVD-29.'
  test: 'ACT-56 test status unexercised: ACT-56 Synchronously review-merges run branch into source branch.
    remains unexercised because no admitted exact test locator asserts its complete record proposition
    under EVD-29.'
  documentation: 'ACT-56 documentation status documented: ACT-56 Synchronously review-merges run branch
    into source branch. is documented at reality/actions/act-56-review-final-merge.yaml and by EVD-29;
    any listed question (no canonical unresolved conflict or question) is explicitly disclosed and does
    not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-56 capability status unknown: ACT-56 Synchronously review-merges run branch into source
    branch. remains unknown as a user capability: CAP-21=gap as reversibility-action; CAP-44=gap as reversibility-action;
    EVD-29 establishes the record carrier but no relevant CAP authority classifies this record as current
    or derived.'
  epistemic: 'ACT-56 epistemic status observed: ACT-56 Synchronously review-merges run branch into source
    branch. is observed from direct/audit authority EVD-29 (EVD-29 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#repository-review-sourceconfiguration-and-file-effects)),
    with no canonical conflict/question qualification; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/runs.py::merge_back_endpoint
test_locators: []
bounded_test_locators: []
