id: ACT-34
title: Reset failed fan-out child to pending
definition: Fan-out retry resets the addressed failed child task to pending.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-10
actor: caller
permission_requirements:
- PER-3
preconditions:
- addressed legacy task is a failed fan-out child
expected_source_version: No client task or run version
required_input:
  run_id: required
  task_id: required
validation:
- legacy execution mode
- fan-out child identity validation
- failed child status validation
durable_effect: Resets the addressed child task from failed to pending and records
  task-status evidence.
resulting_state_id: STA-8
failure_modes:
- 404 run or child identity
- 409 task is not a failed fan-out child
stale_state_behavior: Targeted update rejects an ineligible child rather than overwriting
  concurrent child work.
idempotency: No idempotency key; a pending child is no longer a legal retry source.
retry_behavior: Refresh the child and parent fan-out state before retrying.
reversibility: No direct undo restores the failed child attempt.
audit_evidence_ids:
- EVD-44
transition:
  from_state_id: STA-15
  to_state_id: STA-8
carrier_relationship:
  command_group: fanout_retry
  paired_effect_action_ids:
  - ACT-35
  affected_carrier: fan-out child task
enforced_authentication: Optional global valid-bearer gate
enforced_authorization: absent
domain_eligibility: Failed legacy fan-out child only.
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-34 implementation status present: ACT-34 Reset failed fan-out child to pending
    is present: SER rows SER-0048 inspect src/orchestrator/workflow/service.py::WorkflowService.retry_fan_out_child
    and establish the record proposition under EVD-44.'
  test: 'ACT-34 test status exercised: ACT-34 Reset failed fan-out child to pending is exercised because
    admitted SER rows SER-0049 contain direct assertions at tests/integration/test_fan_out.py::TestChildTaskStateManagement::test_retry_fan_out_child_records_retry_generation;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-34 documentation status documented: ACT-34 Reset failed fan-out child to pending
    is documented at reality/actions/act-34-fanout-child-pending.yaml and by EVD-44; any listed question
    (Q-5 (open: Define product-role authorization before treating actor text as authority)) is explicitly
    disclosed and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-34 capability status unknown: ACT-34 Reset failed fan-out child to pending remains
    unknown as a user capability: CAP-27=unknown as action-contract; CAP-49=gap as action-contract; CAP-59=gap
    as action-contract; CAP-88=unknown as action-contract; CAP-107=unknown as action-contract; EVD-44
    establishes the record carrier but no relevant CAP authority classifies this record as current or
    derived.'
  epistemic: 'ACT-34 epistemic status observed: ACT-34 Reset failed fan-out child to pending is observed
    from direct/audit authority EVD-44 (EVD-44 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#legacy-task-approval-clarification-and-retry-capability)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/workflow/service.py::WorkflowService.retry_fan_out_child
test_locators:
- tests/integration/test_fan_out.py::TestChildTaskStateManagement::test_retry_fan_out_child_records_retry_generation
bounded_test_locators: []
