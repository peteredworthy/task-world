id: ACT-28
title: Skip manual-gate step and resume legacy run
definition: Skips the current manual-gate step and performs the immediate paused-to-active
  legacy run transition before later progression evaluation.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-33
actor: caller
permission_requirements:
- PER-3
preconditions:
- legacy run is paused with pause reason manual_gate
- target is the current actionable step
expected_source_version: No client step or run version
required_input:
  run_id: required
  step_id: required
validation:
- manual-gate paused-run validation
- current actionable-step validation
durable_effect: Records StepSkipped, completes the skipped step, advances its index,
  and records the immediate run resume; later progression can independently pause
  at another manual gate.
resulting_state_id: STA-2
failure_modes:
- 404 run or step identity
- 409 run is not manual-gate paused
- 409 target is not the current actionable step
stale_state_behavior: A changed run pause reason or actionable step is rejected.
idempotency: No idempotency key; a completed or non-current step cannot be skipped
  again.
retry_behavior: Refresh run and current-step state before retrying.
reversibility: No exact undo; transition-back does not restore all file or external
  effects.
audit_evidence_ids:
- EVD-7
- EVD-12
transition:
  from_state_id: STA-4
  to_state_id: STA-2
enforced_authentication: Optional global valid-bearer gate
enforced_authorization: absent
domain_eligibility: Current legacy step while its run is paused at manual_gate.
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-28 implementation status present: ACT-28 Skip manual-gate step and resume legacy
    run is present: SER rows SER-0035 inspect src/orchestrator/api/routers/runs.py::skip_step and establish
    the record proposition under EVD-7, EVD-12.'
  test: 'ACT-28 test status exercised: ACT-28 Skip manual-gate step and resume legacy run is exercised
    because admitted SER rows SER-0036 contain direct assertions at tests/integration/test_api_runs.py::test_skip_step_writes_step_skipped_to_events_v2_and_activity;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-28 documentation status documented: ACT-28 Skip manual-gate step and resume legacy
    run is documented at reality/actions/act-28-step-skip-resume.yaml and by EVD-7, EVD-12; any listed
    question (Q-5 (open: Define product-role authorization before treating actor text as authority)) is
    explicitly disclosed and does not create contradictory documentation for this bounded proposition.'
  capability: 'ACT-28 capability status unknown: ACT-28 Skip manual-gate step and resume legacy run remains
    unknown as a user capability: no CAP implementation_carrier_binding; EVD-7, EVD-12 establishes the
    record carrier but no relevant CAP authority classifies this record as current or derived.'
  epistemic: 'ACT-28 epistemic status observed: ACT-28 Skip manual-gate step and resume legacy run is
    observed from direct/audit authority EVD-7, EVD-12 (EVD-7 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#state-carriers-and-legal-transitions);
    EVD-12 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#state-carriers-and-legal-transitions)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/runs.py::skip_step
test_locators:
- tests/integration/test_api_runs.py::test_skip_step_writes_step_skipped_to_events_v2_and_activity
bounded_test_locators: []
