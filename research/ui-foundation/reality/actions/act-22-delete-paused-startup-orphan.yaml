id: ACT-22
title: Qualify paused startup orphan for deletion
definition: Identify the route's narrow paused startup-orphan eligibility before the
  delete route returns its separate deleted-response carrier.
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-7
actor: caller
permission_requirements:
- PER-1
- PER-2
- PER-3
preconditions:
- legacy run is paused
- run satisfies the startup-orphan predicate
expected_source_version: No client run version.
required_input:
  run_id: required
validation:
- status-limited deletion
- startup-orphan predicate
durable_effect: Qualifies a paused startup orphan as deletable; the synchronous delete
  route then returns deleted response, and later readback observes absence.
resulting_state_id: STA-70
failure_modes:
- 404 missing
- 409 paused run is not a startup orphan
- 409 active or ordinary paused run is not deletable
stale_state_behavior: A concurrent lifecycle change or startup completion can invalidate
  the orphan predicate and reject deletion before a deleted response is produced.
idempotency: No idempotency key. After deletion, retry returns missing rather than
  repeating the response.
retry_behavior: Refresh row status and startup-orphan conditions before retrying;
  do not retry a destructive request blindly.
reversibility: Irreversible after the separate deleted response; no restore route
  is established.
audit_evidence_ids:
- EVD-43
- EVD-38
- EVD-39
- EVD-40
transition:
  from_state_id: STA-4
  to_state_id: STA-70
eventual_deleted_response_transition:
  from_state_id: STA-70
  to_state_id: STA-58
  evidence_ids:
  - EVD-43
eventual_absence_transition:
  from_state_id: STA-58
  to_state_id: STA-56
  evidence_ids:
  - EVD-43
enforced_authentication: Optional global valid-bearer gate; disabled by default.
enforced_authorization: absent
domain_eligibility: Only a paused row satisfying the narrow startup-orphan predicate;
  ordinary paused rows are excluded.
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: exercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-22 implementation status present: ACT-22 Qualify paused startup orphan for deletion
    is present: SER rows SER-0024 inspect src/orchestrator/api/routers/runs.py::delete_run and establish
    the record proposition under EVD-43, EVD-38, EVD-39, EVD-40.'
  test: 'ACT-22 test status exercised: ACT-22 Qualify paused startup orphan for deletion is exercised
    because admitted SER rows SER-0025 contain direct assertions at tests/integration/test_api_runs.py::test_delete_startup_orphan_run;
    no active rejected locator defeats that proof.'
  documentation: 'ACT-22 documentation status documented: ACT-22 Qualify paused startup orphan for deletion
    is documented at reality/actions/act-22-delete-paused-startup-orphan.yaml and by EVD-43, EVD-38, EVD-39,
    EVD-40; any listed question (Q-5 (open: Define product-role authorization before treating actor text
    as authority)) is explicitly disclosed and does not create contradictory documentation for this bounded
    proposition.'
  capability: 'ACT-22 capability status unknown: ACT-22 Qualify paused startup orphan for deletion remains
    unknown as a user capability: no CAP implementation_carrier_binding; EVD-43, EVD-38, EVD-39, EVD-40
    establishes the record carrier but no relevant CAP authority classifies this record as current or
    derived.'
  epistemic: 'ACT-22 epistemic status observed: ACT-22 Qualify paused startup orphan for deletion is observed
    from direct/audit authority EVD-43, EVD-38, EVD-39, EVD-40 (EVD-43 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#run-and-lifecycle-capability);
    EVD-38 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#authority-model);
    EVD-39 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#authority-model);
    EVD-40 (audit-report: research/ui-foundation/agent-reports/03-workflow-state.md#legacy-versus-graph-mode)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/runs.py::delete_run
test_locators:
- tests/integration/test_api_runs.py::test_delete_startup_orphan_run
bounded_test_locators: []
