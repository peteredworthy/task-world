id: ACT-59
implementation_status: present
capability_status: unknown
executable: true
command_id: CMD-28
actor: caller
permission_requirements:
- PER-1
- PER-7
preconditions:
- worktree exists
- unresolved conflicts exist
- runner dispatch available
expected_source_version: None
required_input:
  run_id: required
  agent_runner_type: optional
  agent_runner_config: optional
validation:
- conflicts
- runner selection
- dispatch
durable_effect: Synchronously accepts background conflict-agent dispatch, emits AgentFixStarted,
  and returns dispatched job_id.
resulting_state_id: STA-39
failure_modes:
- 409 no conflicts/dispatch unavailable
- invalid runner
- later agent failure
stale_state_behavior: Dispatch is not a resolved conflict.
idempotency: No key; repeat can dispatch another job.
retry_behavior: Refresh conflicts/runner.
reversibility: No dispatch undo.
audit_evidence_ids:
- EVD-48
transition:
  from_state_id: STA-38
  to_state_id: STA-39
enforced_authentication: optional JWT
enforced_authorization: absent
domain_eligibility: PER-7 review prerequisites
proposed_role_policy: null
conflict_ids: []
question_ids: [Q-5]
test_status: unexercised
documentation_status: documented
epistemic_status: observed
status_basis:
  implementation: 'ACT-59 implementation status present: ACT-59 Synchronously accepts background conflict-agent
    dispatch, emits AgentFixStarted, and returns dispatched job_id. is present: SER rows SER-0098 inspect
    src/orchestrator/api/routers/review.py::agent_resolve_conflicts and establish the record proposition
    under EVD-48.'
  test: 'ACT-59 test status unexercised: ACT-59 Synchronously accepts background conflict-agent dispatch,
    emits AgentFixStarted, and returns dispatched job_id. remains unexercised because no admitted exact
    test locator asserts its complete record proposition under EVD-48.'
  documentation: 'ACT-59 documentation status documented: ACT-59 Synchronously accepts background conflict-agent
    dispatch, emits AgentFixStarted, and returns dispatched job_id. is documented at reality/actions/act-59-agent-conflict-resolution-job.yaml
    and by EVD-48; any listed question (Q-5 (open: Define product-role authorization before treating actor
    text as authority)) is explicitly disclosed and does not create contradictory documentation for this
    bounded proposition.'
  capability: 'ACT-59 capability status unknown: ACT-59 Synchronously accepts background conflict-agent
    dispatch, emits AgentFixStarted, and returns dispatched job_id. remains unknown as a user capability:
    no CAP implementation_carrier_binding; EVD-48 establishes the record carrier but no relevant CAP authority
    classifies this record as current or derived.'
  epistemic: 'ACT-59 epistemic status observed: ACT-59 Synchronously accepts background conflict-agent
    dispatch, emits AgentFixStarted, and returns dispatched job_id. is observed from direct/audit authority
    EVD-48 (EVD-48 (audit-report: research/ui-foundation/agent-reports/04-api-actions-authority.md#repository-review-sourceconfiguration-and-file-effects)),
    qualified by conflicts/questions [''Q-5'']; only the bounded record proposition is observed.'
implementation_locators:
- src/orchestrator/api/routers/review.py::agent_resolve_conflicts
test_locators: []
bounded_test_locators: []
