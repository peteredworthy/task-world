id: ACT-970
implementation_status: present
capability_status: current
executable: true
command_id: CMD-905
actor: "REST caller; graph lifecycle application is a mode-specific CMD-905 consequence, not product-role authorization."
permission_requirements: [PER-900, PER-901, PER-902, PER-903]
preconditions: ["ACT-932 accepted REST cancellation from an active legacy row.", "The addressed graph run is active and graph mode cancellation applies."]
expected_source_version: "The REST cancel request supplies no client row or graph position; REST acceptance preserves the active legacy row while this graph-run carrier transitions separately."
required_input: {run_id: required}
validation: ["CMD-905 validates active legacy-row cancellation and enqueues its signal", "PER-903 validates graph lifecycle cancellation from active"]
durable_effect: "Graph cancellation transitions the graph-run carrier from active to cancelling; terminal graph cancellation is the paired ACT-966 consequence."
resulting_state_id: STA-921
failure_modes: ["404 missing run/graph", "409 stopping or illegal cancellation state", "executor cancellation failure"]
stale_state_behavior: "A duplicate or delayed cancellation observes the graph lifecycle state; it does not transition the graph run into a legacy row carrier."
idempotency: "No idempotency key; graph lifecycle state determines whether a repeat is eligible."
retry_behavior: "Read REST activity and graph-run state; do not treat 202 acceptance as proof that graph cancellation is terminal."
reversibility: "No direct uncancel; later recovery is separate and is not reversal."
audit_evidence_ids: [EVI-904, EVI-913, EVI-931, EVI-932, EVI-933]
transition: {from_state_id: STA-917, to_state_id: STA-921}
carrier_relationship: {kind: paired graph-run entry consequence, paired_graph_run_terminal_action_id: ACT-966, paired_node_consequence_action_id: ACT-967, rest_acceptance_action_id: ACT-932, rest_acceptance_transition: {from_state_id: STA-901, to_state_id: STA-901}, rest_eventual_application_transition: {from_state_id: STA-901, to_state_id: STA-906}, ordering: "CMD-905 acceptance preserves the active legacy row; this graph run enters cancelling, then ACT-966 reaches graph-cancelled, while ACT-967 handles eligible node cancellation on its own carrier."}
enforced_authentication: "PER-900 optional global valid-bearer gate."
enforced_authorization: absent
domain_eligibility: "PER-902 active legacy-row cancel eligibility and PER-903 graph active-to-cancelling eligibility."
proposed_role_policy: null
